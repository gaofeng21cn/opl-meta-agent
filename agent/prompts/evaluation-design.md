# Evaluation design

Use `oma-target-assessment-eval-design` to embed an `EvalSpec` in the blueprint. Define public cases, protected requirement categories, measurable gates, baseline comparison, and mandatory independent evaluation. Preserve existing tests and thresholds for takeover/improve.

Return the complete final `AgentBlueprint`; do not execute evaluation or claim qualification.
