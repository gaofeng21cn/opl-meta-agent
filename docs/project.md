# Project

Owner: `oma`
Purpose: `domain_agent_project_overview`
State: `active_truth`
Machine boundary: Human-readable objective and scope. Machine truth lives in `contracts/`, `agent/`, tests, and repository verification output.

OMA turns an Agent engineering objective into a complete, testable semantic design and turns independent evidence into a complete next design. OPL makes that design executable, measurable, versioned, reversible, and operationally controlled.

The project succeeds when a user can use one entry to create, take over, or improve an Agent while OMA and OPL maintain a single unambiguous authority boundary.
