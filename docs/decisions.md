# meta-agent 决策

- Adopt OPL standard domain-agent scaffold v1.
- Keep this repo as a domain authority pack plus thin program surface.
- Use `meta-agent` as the repo/product wording.
- Keep `meta-agent` outside the OPL Framework repo; OPL provides Agent Lab and scaffold surfaces only.
