# opl-meta-agent 私有实现与 OPL 迁移台账

Owner: `opl-meta-agent`
Purpose: `opl_private_implementation_migration_inventory`
State: `active_inventory`
Machine boundary: 本文是 human-readable 迁移治理台账。机器真相继续归 `contracts/`、`runtime/authority_functions/`、scripts 行为、Agent Lab result refs、developer work order refs、candidate refs、owner receipts、target owner receipts / typed blockers、source 和 tests。

## 当前 clean truth

`opl-meta-agent` 是 OPL-compatible Foundry Agent / builder agent，不是 OPL Framework 内置模块。OPL Framework 持有 standard scaffold、Agent Lab、generated interface bundle、runtime、queue、attempt ledger、provider receipt、observability、registry、App workbench、work-order execute/absorb/cleanup 和 promotion gate。

`opl-meta-agent` 只保留 agent-building semantic pack、refs-only action outputs、candidate package / developer work-order / mechanism proposal materialization 和 minimal authority functions。Purpose-first owner-delta gate 要求这些输出先回答 target-agent 语义增量是什么、下一 owner 欠什么 delta / receipt / gate / blocker；没有明确 owner route、next forced delta、verification/no-forbidden-write 或 target owner receipt/typed blocker 时，只能返回 typed blocker。

本文只维护当前私有实现分类、active caller、保留 authority、可上收 OPL 子域和退役门槛；不保存分支名、逐日拆分过程、执行流水、proof snapshots 或完整 per-file closeout。退役门槛的机器入口是 `runtime/authority_functions/meta-agent-authority-functions.json#script_morphology_policy/script_to_pack_retirement_gates` 与 source-purity tests；本文解释 gate 语义，不替代机器合同。已移出的过程叙事只通过 [过程历史索引](../history/process/README.md) 读取。

## Machine evidence entrypoints

本文不冻结 live run output、receipt counts、branch/worktree closeout、line-count snapshot 或 dated proof。需要确认当前状态时，从以下入口 fresh-read：

- Source shape and no-resurrection boundary：`contracts/functional_privatization_audit.json`。
- Source-purity and script-to-pack gates：`runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt` / `#script_morphology_policy`、`contracts/script_to_pack_gate_receipt.json`、`contracts/source_structure_policy.json#script_to_pack_receipt_guard`、`contracts/private_functional_surface_policy.json#allowed_opl_surface_consumption_refs`、`tests/source-purity.test.ts`、`tests/source-purity/*.test-case.ts`、`tests/support/source-purity.ts`、`tests/source-purity-boundary.test.ts`、`npm run script-to-pack:readback` 和 `npm run script-to-pack:readback:full`。这些 machine surfaces 持有 active caller scan、source-ref integrity、false-ready scan、cleanup readback、generic materializer no-resurrection、reciprocal consumption policy、source-structure receipt drift guard、self-guard exclusion 和 allowed OPL surface consumption 的字段级规则。本文只解释迁移语义：repo-local `.ts` / `.sh` scripts 必须有真实 non-self caller、repo-local source refs、allowed class、false-authority flags、retention/retirement gate 和 tombstone/provenance route；readback 只能暴露 candidate、missing evidence、owner-delta route 和 typed-blocker ref shape，不能授权 physical delete、script retirement、OPL primitive parity、App/registry/generated readiness、default promotion、target/domain/production readiness、typed blocker instance 或 owner receipt body write。`meta-agent-authority-functions.json` 是 generated consumer aggregate；需要改 authority truth 时编辑 `.source.json`、`.leaf-index.json` 和 `meta-agent-authority-functions.parts/**` 后运行 `npm run authority-functions:write` / `npm run authority-functions:check`。
- Generated/default caller consumption：OPL `agents interfaces --repo-dir <this-repo> --json`。
- Production-consumption refs-only ledger：OPL `runtime oma-production-consumption list --json`。
- Active script caller surface：`package.json#scripts`、TypeScript imports, shell invocations and test refs outside `tests/source-purity.test.ts` self-guard strings.
- Split-pressure audit：fresh line-count scan over `scripts/`, `tests/`, `contracts/`, `runtime/` and `agent/`.
- Rational refactor scope audit：first match the requested write-scope paths against tracked files. If the requested Python source/test paths are absent and only analogous TypeScript / JSON surfaces exist, treat the lane as `no_safe_semantic_split` until the write scope is explicitly realigned; do not infer permission to edit adjacent TS/JSON files.

这些入口只能证明 source shape、refs-only consumption、active caller 和 split-pressure。它们不授权 OMA 持有 generated/default shell、target truth、memory/artifact body、owner receipt、domain ready、production ready、quality/export verdict、default promotion、private runner、promotion engine、workbench、scaffold generator、queue、attempt ledger、worktree lifecycle owner、target owner closeout hook owner 或 App/registry owner。

当前脚本治理口径：

`agent/professional_skills/oma-work-order-hygiene/SKILL.md` 是该治理面的 AI-first reviewer；旧 `oma-script-to-pack-hygiene-reviewer` 只保留 redirect。该 workflow-level skill 读取 script-to-pack readback、source-purity tests、本文、script morphology policy 和 active caller evidence，输出 retain / absorb / delete / tombstone / route-back / work-order recommendation。它只提供专业判断和推荐分类，不授权 physical delete、owner receipt、typed blocker instance、OPL primitive parity、App/generated readiness、default promotion、target/domain/production readiness 或 script retirement。

| Risk surface | Current reading | Gate |
| --- | --- | --- |
| `agent:evidence` / `improve:external-suite` | `agent:evidence` 只生成 declarative suite seed、Foundry evaluation work order 和 candidate refs；`improve:external-suite` 只消费显式 suite result 并生成 capability judgment、developer work order 或 expected blocker ref。 | 两者都不得调用 Agent Lab runner、写 result/receipt/ledger/blocker body、直接 patch target 或持有 worktree/promotion/owner-closeout authority；target improvement policy 只消费 target-owned change refs / mappings。 |
| `build-agent-baseline` | 默认启动 live Codex `stage-decomposition` attempt，或消费显式 typed closeout packet，调用 strict materializer 与 OPL scaffold/interface projection，再写 AgentBuildReceipt、declarative suite seed 和 Foundry evaluation work order。 | 不能恢复 local Agent Lab runner/result/receipt/ledger、action-level fake delivery gate、no-`--domain-id` implicit fixture smoke、no-closeout implicit fixture graph、fallback route 或 target pack authority。 |
| `stage-decomposition` helpers | `stage-decomposition-runner` 是 OPL stage attempt adapter；旧 `scripts/lib/stage-decomposition-pack-draft.ts` barrel re-export 已物理退役，active callers 直接 import `scripts/lib/stage-decomposition-pack-draft/{builder,validator,materializer,shared}.ts`。 | 该目录按 target pack draft 的自然边界拆成 fixture builder、pack validator、target repo materializer、stage-native ref template 和 shared contract consumer；Stage Native Artifact 稳定词表由 `contracts/stage_native_artifact_vocabulary.json` 持有。它仍只能作为 retained refs-only helper/materializer，不能恢复 public export entry / compatibility path，也不能扩成 scaffold generator、physical kernel runtime state owner、conformance gate owner、workbench owner、owner promotion path 或 target worktree lifecycle owner。 |
| `meta-agent-loop*` helpers | 旧 `meta-agent-loop.ts` re-export facade 已退役；IO/OPL client、AI reviewer gate与 declarative suite-seed builder 由相邻 helper 直接承接。 | 保留 direct helper，不恢复 compatibility facade、receipt/ledger builder 或 private runner。 |
| Retired local wrappers | legacy repo-local Codex plugin/Skill wrapper、installer test、runtime sidecar/projection/lifecycle placeholder 和 `bootstrap-domain-packs` 已退役；当前 `plugins/opl-meta-agent/` 只允许作为 primary skill 的 full-copy carrier。 | 不恢复 `.agents/`、`.codex/`、repo-local CLI/MCP/Skill/product-entry/sidecar/status/workbench wrapper 或 compatibility materialization route；`plugins/opl-meta-agent/` 不能扩成 generic wrapper 或第二 package truth。 |

## Classification

| class | 含义 |
| --- | --- |
| `domain_authority_retained` | agent-building 语义、candidate/work-order/proposal materialization refs；不能写目标 domain truth。 |
| `opl_framework_migration_candidate` | 当前脚本内承担的 registry/App/Agent Lab/promotion/runtime/queue/attempt shell 语义，长期归 OPL。 |
| `already_thin_adapter` | smoke/proof/helper/materializer，仍有 repo-local script caller，但已声明 no generic owner；增长后必须继续拆薄、迁入 pack/contracts/authority refs 或上收 OPL primitive。 |
| `stage_attempt_adapter` | 只适配 OPL stage attempt surface；不能实现私有 queue/daemon/attempt ledger。 |
| `strict_materializer_natural_boundary` | 只校验/物化 typed closeout 或 refs-only packet；继续增长时按 target pack draft 的真实责任边界拆分，而不是按 `*-parts` 实现切片拆分。 |

## 当前脚本治理口径

| surface | class | current active caller | 当前实际职责 | 为什么属于该分类 | OMA 必须保留的 authority | 可迁往 OPL 的 generic 子域 | 迁移/退役门槛 | 推荐验证入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `scripts/lib/agent-evidence-materializer.ts` | `already_thin_adapter` | `npm run agent:evidence`、`tests/agent-evidence-materializer.test.ts` | 读取 target production-evidence contracts，生成 declarative suite seed、target-bound Foundry evaluation work order、candidate refs 或 expected blocker ref | contract intake 已拆到 `agent-evidence-contracts.ts`；materializer 不调用 Agent Lab、不写 result/receipt/ledger/blocker body，不扩成 evidence runner / promotion gate | target-agent production-evidence evaluation handoff refs | Agent Lab execution/result/receipt ledger、promotion gate、target-owner return primitive、App workbench/registry consumption | stable evaluation vocabulary 继续迁入 pack/contract 或 OPL primitive；不得恢复 local runner、receipt writer 或 target lifecycle | `npm test`、`npm run typecheck` |
| `scripts/improve-from-agent-lab-suite.ts` | `already_thin_adapter` | `npm run improve:external-suite`、external-suite tests | 读取 explicit suite + suite result + reviewer evidence，生成 capability judgment、developer patch work order 或 expected blocker ref | 不调用 Agent Lab、不直接 patch target、不写 target version/owner receipt、typed-blocker body 或 hosted ledgers；work order 必须显式命中 target-owned capability map / policy | target-agent source patch work-order/capability/expected-blocker refs | target patch execution、Agent Lab regression、promotion gate、worktree lifecycle / absorb / cleanup、owner closeout | 保持 independent reviewer fail-closed 与 target-generic route；不接管 runner、patch lifecycle 或 target owner hook | `npm test`、`npm run typecheck` |
| `scripts/lib/agent-evidence-contracts.ts` | `already_thin_adapter` | imported by `agent:evidence` | 参数解析、target agent production acceptance 定位、required target contract loading 和 repo-relative ref normalization | 是从 `agent-evidence-takeover.ts` 拆出的 contract intake helper；不运行 Agent Lab、不签 target owner receipt、不输出 work order、不持有 target truth/verdict/artifact/memory body | target-agent contract intake refs | OPL generated/default caller、target-agent handoff discovery、Agent Lab intake primitive | 保持 contract-loading helper；不要扩成 generic registry/discovery owner、status reader 或 workbench shell | `npm test`、`npm run typecheck` |
| `scripts/lib/external-suite-materializer.ts` | `already_thin_adapter` | imported by `improve:external-suite` | developer patch work-order、machine closeout refs、runtime/workspace verification refs、no-forbidden-write refs 和 external-suite candidate artifact 写出 | refs-only materializer 不运行 suite、不签 receipt、不写 blocker body、不持有 target truth/verdict/artifact/memory；缺 inputs 时由 caller只返回 expected blocker ref | target-agent external-suite work-order materialization refs | OPL work-order readiness/execution、target-owner return、promotion、worktree lifecycle 和 App/workbench read model | stable vocabulary 进入 `agent/`、`contracts/` 或 OPL primitive；helper 不扩成 runner / queue / gate / lifecycle owner | `npm test`、`npm run typecheck` |
| `scripts/build-agent-baseline.ts` | `already_thin_adapter` | `npm run build-agent-baseline`、`tests/bootstrap-loop.test.ts`、`tests/stage-decomposition-materializer.test.ts` | stage-decomposition orchestration、strict pack materialization、OPL scaffold/interface projection、AgentBuildReceipt、declarative suite seed 和 Foundry evaluation work order | 不再决定 target stage graph，也不调用 Agent Lab runner或写 baseline/owner receipt、learning/scaleout ledger、promotion result；fixture runner 必须带 explicit closeout | candidate Agent Pack、AgentBuildReceipt、evaluation handoff、stage-decomposition blocker refs | stage attempt read model、standard scaffold/generator、Agent Lab execution/result/receipt ledger | 保持 action implementation / smoke helper；不得恢复 implicit fixture/default graph、private runner、fake delivery gate 或 compatibility facade | `npm run typecheck`、`npm test`、`npm run verify` |
| `scripts/lib/domain-pack.ts` | `already_thin_adapter` / `fixture_or_proof_helper` | imports from baseline、takeover、external-suite、work-order helpers and tests | 读取当前 `agent/` domain pack summary；为 generated target-agent smoke 写最小 `agent/` pack fixture 和 morphology conformance fixture | 这是共享 JSON/type 和 deterministic fixture helper，只能产生 pack summary / fixture refs；不能成为 OPL pack compiler、generated interface owner、scaffold generator owner、target pack authority 或 target truth writer。生成的 morphology fixture 明确 `generated_interface_owner="one-person-lab"`，并把 target truth、memory/artifact body、quality/export verdict 和 default promotion flags 全部置 false | domain pack summary refs、target-agent minimal pack fixture refs、generated morphology conformance fixture boundary refs | OPL pack compiler / standard scaffold generator / generated interface bundle owner | OPL pack compiler target-pack fixture parity、no pack compiler/generated interface owner proof、no active caller 和 tombstone/provenance refs 具备前才可退役；新增 pack helper 职责必须先进入 source-purity classification 和 private policy forbidden-role guard | source-purity tests、`tests/bootstrap-loop.test.ts`、`npm test` |
| `scripts/lib/stage-decomposition-pack-draft/` | `strict_materializer_natural_boundary` | imported directly by `build-agent-baseline` and stage-decomposition tests | typed closeout fixture builder、pack draft validator、target repo pack file / contract materializer、Stage-Native refs-only template materializer and shared refs/helpers | 只校验并物化 Codex closeout 提供的 stage graph/action refs/files/gates 和 stage-native ref templates；不启动 runtime、不签 receipt、不生成默认 graph、不创建 stage folder runtime state、不 owner promotion、不管理 target worktree、不写 target owner receipt body；旧 `scripts/lib/stage-decomposition-pack-draft.ts` barrel re-export 已退役，当前目录按自然边界承接 builder / materializer / shared contract consumer / validator / artifact morphology validator / stage-native contract validator，全部纳入 morphology + script-to-pack gate；stage pack defaults、shared policy release 和 forbidden generic owner roles 已从 `shared.ts` 硬编码迁入 `contracts/standard_foundry_policies.json`，Stage Native Artifact 稳定词表已迁入 `contracts/stage_native_artifact_vocabulary.json`，helper 只作为 contract consumer | stage-decomposition pack validation/materialization refs、artifact morphology validation refs、physical kernel locator / conformance / workbench consumption ref templates、standard Foundry policy contract consumer refs、stage-native vocabulary contract consumer refs | OPL pack compiler materializer / stage packet closeout validator / artifact morphology contract validator / physical kernel locator / conformance / App workbench consumption primitive | OPL Framework 提供同等 typed packet validator/materializer，并有 OPL physical kernel / conformance / workbench consumption parity、no OMA runtime state / owner promotion / worktree / receipt body proof 后，本 helper 组降为 explicit fixture/proof helper 或退役；不得扩成 scaffold generator | `npm run typecheck`、`npm test`、`npm run verify` |
| `scripts/lib/stage-native-artifact-contract.ts` | `contract_consuming_refs_only_materializer` | imported by stage-decomposition builder/validator and stage-decomposition tests | builds Stage-Native Artifact Contract refs and refs-only attempt template contracts from `contracts/stage_native_artifact_vocabulary.json` | 只生成 artifact/native stage folder, manifest, receipt, blocker, pointer, canonical/export/lineage/retention, physical kernel locator, conformance and workbench consumption refs；稳定 vocabulary、required fields、required files、output roles 和 false-authority flags 归 contract；不读写 runtime state、target artifact body、owner promotion、target worktree or receipt body | stage-native artifact contract and ref template vocabulary contract consumer refs | OPL stage artifact runtime contract compiler / physical kernel locator / conformance / workbench projection primitive | Keep pure refs builder; retire or absorb only after OPL compiler parity and no active caller evidence | `contracts/stage_native_artifact_vocabulary.json`、`tests/stage-decomposition-materializer.test.ts`、`tests/contracts-stage-control.test.ts` |
| `scripts/lib/stage-decomposition-runner.ts` | `stage_attempt_adapter` | imported by `build-agent-baseline` and stage-decomposition tests | 写 stage packet，启动 live stage-decomposition attempt 或消费显式 fixture closeout，写 attempt receipt | 只适配 OPL stage attempt surface；live path 调用 OPL family-runtime，fixture path 必须带 closeout packet 且只用于测试/proof；不实现私有 queue/daemon/attempt ledger | stage-decomposition attempt receipt refs、fixture_closeout_packet_required typed blocker | OPL family-runtime stage attempt primitive / read model | OPL 生成 action implementation 可直接启动/查询 stage attempt 后，本 adapter 可收薄或退役；无 closeout fixture graph 已物理退役，不可恢复 | `npm run typecheck`、`npm test`、`npm run verify` |
| `scripts/lib/meta-agent-loop-io.ts` | `already_thin_adapter` with thinner migration input | direct imports from action/materializer scripts | `DEFAULT_OPL_BIN`、OPL CLI JSON call、stable id、JSON read/write、target agent descriptor read | 从退役聚合 helper 拆出的 IO / OPL client helper；只消费 OPL CLI，不实现 Agent Lab runner 或 runtime | OPL invocation helper refs、target-agent descriptor intake | OPL-owned invocation shell / generated interface caller | retain as small helper until OPL caller parity can replace local script usage | `npm test`、`npm run typecheck` |
| `scripts/lib/meta-agent-loop-ai-reviewer.ts` | `already_thin_adapter` with thinner migration input | direct imports from action/materializer scripts | AI reviewer evaluation type、validation、load、receipt fields、acceptance gates | reviewer gate shape 从退役聚合 helper 拆出；只做 fail-closed validation 和 receipt field projection，不签 quality verdict | AI reviewer evidence/gate projection refs | OPL Agent Lab reviewer pool / review-attempt receipt read model | stable reviewer vocabulary 继续迁入 `agent/`、`contracts/` 或 Agent Lab primitive；不授权 promotion | `npm test`、`npm run typecheck` |
| `scripts/lib/meta-agent-loop-receipts.ts` | `already_thin_adapter` with authority implementation refs | direct imports from baseline / takeover / agent-evidence scripts | declarative Agent Lab suite-seed builder | helper 只生成 task/recovery/scorecard/improvement/promotion request specs；不写 observations、result、receipt、learning/scaleout ledger 或 promotion state | suite-seed materializer helper | Agent Lab execution/result/receipt ledger、runtime/queue/attempt ledger、promotion gate | keep narrow; stable hosted vocabulary belongs OPL; do not restore receipt/ledger builders or `scripts/lib/meta-agent-loop.ts` facade | `npm test`、`npm run typecheck` |
| `scripts/takeover-agent.ts` | `already_thin_adapter` / `fixture_or_proof_helper` | `npm run takeover:test`、`tests/takeover-loop.test.ts` | 读取 target descriptor、消费 AI reviewer evidence，生成 declarative takeover suite seed、target-bound Foundry evaluation work order 和 proposal-only candidate refs | 不调用 Agent Lab、不写 suite result、takeover/owner receipt、learning ledger、promotion result 或 `new_agent_delivery_gate` readback；完整 target identity 与 provenance 进入 stable work-order identity | suite-seed ref、Foundry evaluation work-order ref、gated self-evolution/mechanism candidate refs | OPL Agent Lab runner/result/receipt ledger、promotion gate、target owner closeout、target worktree lifecycle | 保持 smoke/helper；不得恢复 `--fixture` / `--opl-bin` alias、local runner、receipt writer 或 action-level fake delivery gate | `tests/takeover-loop.test.ts`、`tests/contracts-action-authority.test.ts`、`tests/contracts-stage-control.test.ts`、`npm run typecheck` |
| `scripts/lib/work-order-{refs,efficiency,builders,validation}.ts` | `already_thin_adapter_with_policy_contract_consumer` | imported by `agent:evidence` and `improve:external-suite` materializers | 共享 target patch-loop closeout refs、runtime/read-model proof、workspace proof、developer work-order contract direct consumer、refs-only work-order completeness builder、developer patch work-order validator、generic refs intake / owner-route normalization / evidence refs extraction、`*_efficiency_handoff_projection` intake 和 OPL work-order primitive refs builder | 旧 `work-order-policy.ts` 已自然拆分；developer work-order 默认 forbidden surfaces、runtime/workspace verification refs 和 closeout evidence policy 已迁入 `contracts/developer_work_order_policy.json`，旧 standalone `work-order-policy-constants.ts` projection helper 已退役；`work-order-refs.ts` 作为 active owner helper 直接消费 contract，不运行 suite、不签 promotion、不持有 target truth；primitive refs 只指向 OPL Framework work-order primitive owner；validator 只做 shape fail-closed，不授权 patch 或 promotion；efficiency handoff 只消费 refs-only projection，不新增 domain-specific command family | developer work-order policy contract consumer、refs-only proof builders、target-agent evidence materializer helpers、shape validation | OPL Agent Lab/App/registry runtime consumption receipts、generic readiness primitive implementation 和 promotion gate | 保持 pure helper；新增稳定合同文本继续迁入 `agent/`、`contracts/` 或 authority refs；不得重新合并成 runner / queue / promotion gate；不得恢复 standalone policy projection helper | `npm test`、`npm run typecheck` |
| `scripts/lib/target-improvement-policy.ts` | `already_thin_adapter_with_policy_contract_consumer` | imported by `improve:external-suite` | 从 target `contracts/capability_map.json`、handoff / production acceptance 显式 change-ref policy 读取 source patch target，生成 proposed change refs、patch traceability matrix、editable surface refs 和 target file hints；通用 owner-receipt / package / typed-blocker 关键词只保留 retired tombstone，不再生成 patch target | 是 refs-only target improvement materializer / contract consumer，不运行 suite、不签 owner receipt、不持有 target verdict；旧内置 generic `external_agent/*` patch-ref fallback 已退役，稳定默认映射也已退役，不能在缺 target-owned policy 时生成 source patch work order | target-agent improvement candidate 的 refs-only mapping / traceability materializer、target-owned capability_map / explicit policy consumer 和 missing-policy typed blocker refs | OPL work-order policy primitive、patch traceability read model、promotion gate | stable vocabulary 继续进入 `contracts/` 或 OPL Agent Lab primitive；helper 不扩成 runner / queue / gate；不得恢复 generic external-agent patch-ref fallback | `npm test`、`npm run typecheck` |
| `contracts/app_workbench_projection.json` | `already_thin_adapter` | contracts tests, OPL App consumption refs | refs/status/receipt/blocker projection contract | 已声明 App/workbench owner is OPL; OMA only supplies refs；不是 repo-owned workbench 或 compatibility facade | OMA work-order/candidate refs | App/workbench display shell | keep refs-only; live App screenshot/evidence from OPL/App side | `tests/contracts-generated-surfaces.test.ts`、`tests/external-suite-patch-loop-projection.test.ts` |
| `contracts/opl_domain_manifest_registration.json` | `already_thin_adapter` | contracts tests, OPL registry/discovery refs | registry discovery metadata | registry owner is OPL; OMA supplies registration refs only | domain descriptor/handoff refs | registry/discovery owner | OPL registry receipt required before live claim | `tests/contracts-generated-surfaces.test.ts` |
| `contracts/default_caller_deletion_evidence.json` | `already_thin_adapter` | contracts tests, OPL generated/default caller read model | default caller typed blocker、no-forbidden-write、tombstone/provenance 和 retained domain authority refs | 只为 OPL generated/default caller consumption 提供 domain-owned boundary evidence；不授权 repo-owned default wrapper、sidecar、workbench、generated shell、compatibility facade 或 physical delete | agent-building semantics、typed blocker refs、no-forbidden-write refs、target domain truth boundary refs | generated/default caller read model、hosted workbench/status projection、sidecar dispatch、physical cutover policy | OPL side consumption receipt 可继续关闭 evidence tail；本仓不得恢复 local wrapper 或把 default caller evidence 写成 active surface owner | `tests/contracts-generated-surfaces.test.ts` |
| `takeover:test --fixture` alias | `physically_absent_retired_tail` | no active caller; no parser branch after retirement | 旧 takeover parser 兼容 alias | 已由 `--agent-dir` 显式 target intake 替代；`--fixture` 现在只走普通 unknown-argument fail-closed 路径，不再保留专门 alias 语义 | none | none | 不得恢复 `--fixture` parser branch、public caller 或 compatibility alias | `tests/takeover-loop.test.ts` |
| `runtime/authority_functions/meta-agent-authority-functions.json` | `domain_authority_retained` generated aggregate | contracts tests, authority function refs, source-structure strict gate | explicit authority refs for package builder and proposal authorizer; generated from `.source.json` / `.leaf-index.json` / `meta-agent-authority-functions.parts/**` | minimal refs-only authority functions belong OMA, but cannot own runtime; aggregate path remains for consumers while source parts carry maintenance truth | candidate package builder, proposal authorizer refs | none except OPL invocation shell | keep no generic owner flags; scripts must match implementation refs; edit source parts and run `npm run authority-functions:check` before claiming no drift | `tests/contracts-action-authority.test.ts`、`tests/contracts-generated-surfaces.test.ts`、`npm run authority-functions:check` |

## Inventory by owner boundary

| Boundary | Active surfaces | OMA 保留 authority | OPL / target owner authority |
| --- | --- | --- | --- |
| Candidate baseline materialization | `scripts/build-agent-baseline.ts`、stage-decomposition runner/materializer、domain-pack helpers | candidate Agent Pack、AgentBuildReceipt、declarative suite seed、Foundry evaluation work-order candidate、OMA-owned blocker refs | OPL standard scaffold/interface owner、Agent Lab runner/result/receipt ledger、target owner closeout、promotion gate |
| External evidence / suite improvement | `scripts/lib/agent-evidence-materializer.ts`、`scripts/improve-from-agent-lab-suite.ts`、materializers、target-improvement helpers | declarative suite seed、Foundry evaluation/developer work order、target capability/mechanism candidate、expected blocker refs | Agent Lab execution/result/receipt ledger、target patch lifecycle、truth/quality/artifact/memory/owner receipt/blocker body、promotion gate |
| External work-order execution | `scripts/execute-external-work-order.ts`、work-order validation/builders | refs-only validation and delegation receipt | OPL work-order execute / absorb / cleanup、target owner closeout hook、execution receipt |
| Generated / registry / App projection | `contracts/generated_surface_handoff.json`、`contracts/opl_domain_manifest_registration.json`、`contracts/app_workbench_projection.json` | domain descriptor、action/stage refs、work-order/candidate refs | OPL generated interfaces、registry/discovery owner、App/workbench shell/live rendering |
| Authority functions | `runtime/authority_functions/meta-agent-authority-functions.json`, `runtime/authority_functions/meta-agent-authority-functions.parts/**` and matching scripts | minimal package builder and proposal authorizer refs | generic runtime, queue, scheduler, observability, promotion gate |

## Retired legacy surfaces

本 active inventory 不再维护逐项退役 surface 表。当前读法是：机器缺席 / no-resurrection truth 归 `runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt`、`script_morphology_policy.script_to_pack_retirement_gates` 和 source-purity tests；人读退役 provenance 归 [`docs/history/process/retired-surface-provenance.md`](../history/process/retired-surface-provenance.md)。

Active 语境只保留当前迁移门：legacy repo-local Codex plugin / Skill wrapper、runtime sidecar/status/workbench shell、direct graph / implicit fixture path、compatibility facade、aggregate re-export facade、generic `external_agent/*` patch-ref fallback 等已退役面不得恢复为 active caller、fallback、alias、facade、wrapper、test-only convenience 或 current owner。当前 `plugins/opl-meta-agent/` 只允许作为 `agent/primary_skill/SKILL.md` 的 materialized carrier。若未来要物理删除仍有 active caller 的脚本、helper、contract 或 workflow，必须先满足对应 machine gate 的 OPL primitive parity、no-active-caller、no-forbidden-write、owner receipt / typed blocker 或 explicit owner decision，以及 tombstone/provenance refs。

## Current migration gates

| Gate | Current handling |
| --- | --- |
| owner-local cleanup candidate readback | Cleanup readback is diagnostic only. Retained scripts with active package-script, import, shell, or test callers stay as materializer/helper/verification-wrapper rows until their gate shows OPL primitive parity or explicit fixture/proof refs, no-active-caller, no-forbidden-write where applicable, and tombstone/provenance evidence. Current readback does not authorize owner-local physical delete. |
| Source-structure command aliases | `line-budget` and `line-budget:strict` npm/shell aliases are retired. Canonical commands are `source-structure` and `source-structure:strict`; `contracts/source_structure_policy.json#compatibility_aliases` is empty and `npm run source-structure:json` must not declare `compatibility_alias_retained`. |
| Test support stdlib facade | Tests import `node:assert/strict`, `node:fs`, `node:os`, `node:path` and `node:child_process` directly. `tests/support/contracts.ts` only keeps repo-specific helpers, shared constants and JSON/file helpers; `tests/support/external-suite-fixtures.ts` keeps only external-suite fixture builders/assertions and reuses `writeJsonFile`. |
| Legacy professional skill redirects | Physical `agent/professional_skills/*/TOMBSTONE.md` redirect files are retired. Legacy redirect identity lives in `contracts/capability_map.json#legacy_professional_skill_redirects` as `legacy-professional-skill:<id>` plus the human README ledger; these entries are not repo paths and carry no independent authority. |
| external work-order delegation retention | `scripts/execute-external-work-order.ts` is a retained authority function for developer patch work-order validation plus OPL `work-order execute` delegation. OPL / target owners keep target worktree lifecycle, absorb/cleanup, closeout hook invocation, and owner receipt body authority. Future retirement still requires OPL generated action parity, no-active-caller, no-forbidden-write proof where applicable, and tombstone/provenance evidence. |
| agent evidence / external suite materializer retention | The `agent_evidence_and_external_suite_materializers` group remains a refs-only materializer surface for declarative suite seeds, Foundry evaluation/developer work-order candidates, target improvement proposals, expected blocker refs, runtime read-model consumption, workspace proof, and no-forbidden-write proof refs. It cannot own target truth, target worktree lifecycle, suite/result/receipt ledgers, blocker bodies, quality verdicts, owner receipts, OPL runner parity, or promotion gates. Future retirement requires OPL primitive parity or explicit fixture/proof refs plus no-active-caller, no-forbidden-write where applicable, target/OPL owner decision when parity is claimed, and tombstone/provenance refs. |
| Work-order policy and efficiency refs | Keep small `scripts/lib/work-order-*` helpers as refs-only builders/validators while stable vocabulary moves to pack/contracts/OPL primitive. Do not recreate a monolithic private work-order policy owner. |
| Target improvement and agent-evidence materialization | Keep `target-improvement-policy.ts`, `agent-evidence-materializer.ts` and `external-suite-materializer.ts` as materializers only. Target improvement default owner-receipt/live-acceptance mappings are retired tombstones under `contracts/developer_work_order_policy.json#target_improvement_default_change_ref_policy`; helpers must consume target-owned `capability_map` or explicit policy before issuing source patch refs. They cannot run Agent Lab, sign owner receipts, write target truth, own promotion gates, become App/workbench read models, or invent generic patch refs when the target agent has not declared explicit change-ref policy. |
| Baseline generation | `build-agent-baseline` remains a smoke/action implementation that consumes live stage attempts or explicit typed closeouts plus OPL scaffold/interface projection, then emits AgentBuildReceipt、declarative suite seed 和 Foundry evaluation work order. It cannot regain implicit fixture/default graph authority or local Agent Lab/result/receipt/ledger authority. |
| Stage-decomposition pack-draft natural boundary | `scripts/lib/stage-decomposition-pack-draft.ts` barrel re-export is retired; active callers import `scripts/lib/stage-decomposition-pack-draft/` directly. `scripts/lib/domain-pack.ts` remains only a pack summary / target-agent fixture helper and inherits the same pack compiler / generated interface owner / target pack authority no-resurrection guard. Future work should reduce retained helper semantics only after OPL primitive parity and no-active-caller proof; do not restore a barrel facade or re-merge builder / validator / materializer into one private script. |
| Meta-agent loop helper tail | Keep direct IO / reviewer / receipt helpers as current surfaces. The old `scripts/lib/meta-agent-loop.ts` re-export facade is retired and must not return as a compatibility import path. Any stable reviewer gate or proposal policy should move to contracts/pack/OPL primitive rather than into another aggregate helper. |
| Repo shell verification wrappers | Keep `scripts/repo-hygiene.sh`, `scripts/run-with-repo-temp-env.sh` and `scripts/verify.sh` only as repo-native verification / hygiene / external-cache-boundary wrappers. They are now tracked by the same machine script-to-pack gate as TypeScript materializers; they cannot become generic CLI wrappers, runtime owners, schedulers, queues, generated interface owners, target truth writers, or default hard line-budget gates. |
| Authority manifest source split | Keep `scripts/sync-authority-functions.ts` only as generated aggregate maintenance helper. `runtime/authority_functions/meta-agent-authority-functions.json` remains the stable consumer surface, but strict source-structure truth lives in `.source.json`, `.leaf-index.json`, `.bundle-manifest.json` and `meta-agent-authority-functions.parts/**`. This helper cannot become generated interface owner, registry owner, runtime owner or authority-function truth owner. |
| Aggregate sync helper parity | `scripts/lib/sync-json-bundle.ts` is the thin shared helper for JSON file IO, stable JSON text, ordered object projection, array assertions, source digest calculation and `--split` / `--check` / `--write` CLI dispatch shared by `scripts/sync-authority-functions.ts` and `scripts/sync-stage-control-plane.ts`. The two npm entrypoints keep their own refs, leaf structure, source digest input selection and generated bundle manifest semantics; the helper has its own script-to-pack retention gate and cannot become a generic framework, generated interface owner, runtime owner or domain truth owner. |
| Wrapper no-resurrection | Do not add repo-local scheduler, queue, attempt ledger, CLI/MCP/Skill/product-entry wrapper, status/workbench shell, registry owner, Agent Lab runner, promotion gate, compatibility facade, or domain-specific command family. |
| Script-to-pack retirement gates | Use the machine gates in `contracts/script_to_pack_gate_receipt.json`, `runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt` / `#script_morphology_policy`, `contracts/private_functional_surface_policy.json#allowed_opl_surface_consumption_refs`, `npm run script-to-pack:readback` / `npm run script-to-pack:readback:full` and source-purity tests before any absorb or retire claim. The current receipt closes the previous missing-machine-gate blocker only; every scanned `.ts` or `.sh` script remains a materializer/helper/verification-wrapper row until the matching gate has OPL primitive parity or explicit fixture/proof refs, no-active-caller, no-forbidden-write where applicable, and tombstone/provenance refs. Newly retained scripts must fail closed unless the machine scan proves real non-self callers and repo-local source refs. Cleanup readbacks and reciprocal consumption are owner-route guards only; they cannot authorize delete, typed blocker instances, owner receipts, OPL primitive parity, App/registry/generated readiness, default promotion or target/domain/production readiness. |

## Cleanup parity retention boundary

当前 cleanup parity 结论是 `retained_with_exact_retirement_gate`，不是 `ready` 或
`parity_complete`。`script-to-pack:readback:full` 的失败若来自 strict
source-structure line-budget gate，只说明 primary shrink 未完成；cleanup
`violation_count=0` / `cleanup_candidate_count=0` 仍只表达 cleanup 维度，不等价于
命令通过或可删。

| Cleanup item | Current gate / readback route | Result | Owner route / required primitive | Tombstone stop condition |
| --- | --- | --- | --- | --- |
| `5` line-budget aliases | 当前 worktree 已删除 `package.json#scripts.line-budget` / `line-budget:strict` 和 `scripts/verify.sh` alias lane；`npm run source-structure:json` 读回应保持 `state=passed` 且 `compatibility_aliases=[]`。 | `retired_alias_surface` | 本仓 source-structure policy / source-purity gate owner。无需 OPL primitive；canonical operator surface 是 `source-structure` / `source-structure:strict`。 | 无 active package/verify/machine receipt alias caller；source-purity scan receipt refreshed without alias callers；active docs 不再声明 compatibility alias retained。 |
| `6` `scripts/execute-external-work-order.ts` | OPL owns the work-order execute primitive and covers Codex worktree execution、dry-run、absorb、cleanup 和 owner closeout readback；OMA `tests/execute-external-work-order.test.ts` 仍覆盖 pre-delegation OMA schema/currentness/progress validation。 | `retained_validation_aperture` | OPL owns executor primitive; OMA owns `opl_meta_agent_developer_patch_work_order.v1` validation aperture until OPL exports equivalent validator or generated action consumes the same schema fail-closed before executor launch. | OPL exported/tested equivalent OMA developer-work-order validator or generated action parity; no active `npm run execute:external-work-order` / action catalog / test caller; source-purity receipt refresh; tombstone/provenance ref. |
| `7` `stage-decomposition-pack-draft/**` and `work-order-*` helpers | OPL owns domain-pack compiler, standard scaffold, Stage Artifact runtime, OMA stage-decomposition admission and typed closeout transport, but does not yet provide full `opl_meta_agent_stage_decomposition_pack_draft.v1` builder/validator/materializer parity or OMA work-order helper schema parity. | `retained_refs_only_materializer_and_helper` | OPL Pack / Stage / WorkOrder owners must provide typed packet validator/materializer, pack draft materializer, artifact morphology validator, Stage-Native refs template compiler, and OMA work-order refs/progress/no-forbidden-write validator before this repo can delete this directory. | OPL primitive parity evidence for each helper class; no active import/test/package caller in `build-agent-baseline`, stage-decomposition tests, `agent:evidence`, `improve:external-suite`, or `execute:external-work-order`; source-purity receipt refresh; tombstone/provenance ref. |

Fixture/test support boundary: `tests/source-purity/script-morphology.test-case.ts` remains intentionally explicit because it guards machine receipt and policy fields. Test-side standard-library re-export facades and duplicate external-suite JSON/repo helpers should stay retired; `tests/support/external-suite-fixtures.ts` reuses the repo-local JSON helper instead of carrying duplicate mkdir/write/return code. This does not change item `6/7` retention state or authorize any retained script deletion.

2026-07-09 retained deletion gate audit: current deletion result is
`no_safe_delete_retained_current`. Fresh `npm run source-structure:json` reports
source-purity/script-to-pack guard passed with 32 scanned/gated scripts, 0 orphan scripts,
0 cleanup candidates, 0 cleanup apply candidates and 0 cleanup violations. The compact
`script-to-pack:readback` remains a strict source-structure command and returns
`failed_source_structure_gate` because of existing line-budget findings, but its cleanup
readback still reports 0 cleanup candidates and 32 retained current rows. Fresh OPL
generated/default caller consumption reports generated interface `ready`, default entry
gate `pass`, `domain_repo_can_own_default_entry=false`, and aligned
`execute-external-work-order` answer shape. Those are default-caller/no-resurrection
evidence only; they do not satisfy retained helper deletion because active callers remain:
`scripts/execute-external-work-order.ts` still has package/test callers,
`stage-decomposition-pack-draft/**` still has `build-agent-baseline` / stage-decomposition
test callers, and `work-order-*` helpers still have agent-evidence / external-suite /
execute-delegation imports. Keep the rows as retained authority/thin adapter until the
matching machine gate has OPL validator/materializer parity, no-active-caller refresh,
no-forbidden-write proof where applicable, tombstone/provenance ref and accepted
owner-answer shape evidence. Do not add alias/facade surfaces to make deletion easier.

## OPL primitive dependencies

- Agent Lab suite runner and evolution/promotion gate.
- Generated interface bundle and standard scaffold generator.
- Pack compiler default domain-pack materializer for bootstrap / real-target smoke specs.
- Registry/discovery and App/workbench drilldown live consumption.
- Stage attempt ledger, queue, provider receipt and observability.
- Target-agent generic handoff vocabulary across MAS/MAG/RCA and new Foundry Agents.

## Forbidden claims

- `opl-meta-agent` work order does not sign target owner receipt.
- Suite pass, schema completeness, generated surface readiness or mechanism proposal cannot promote default agent.
- OMA cannot write target domain truth, memory body, artifact body, quality/export verdict or owner receipt authority.
- Registration/App projection readiness is not live App rendering evidence.

## Dynamic evidence entrypoints

- Source-shape and functional privatization: `contracts/functional_privatization_audit.json`.
- Source-purity / script-to-pack dynamic entrypoints: `contracts/script_to_pack_gate_receipt.json`、`runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt` / `#script_morphology_policy`、`npm run script-to-pack:readback` / `npm run script-to-pack:readback:full` and source-purity tests. These surfaces own active-caller, source-ref, false-ready, cleanup-readback, generic-materializer, self-guard and reciprocal-consumption details;本文只保持迁移分类和 non-claim rule，不复制每个 machine field。
- Generated/default caller consumption: OPL `agents interfaces --repo-dir <this-repo> --json`.
- OMA production-consumption refs-only ledger: OPL `runtime oma-production-consumption list --json`.
- Script caller surface: `package.json#scripts`, `tests/*.test.ts` and TypeScript imports.
- Large-file/split-pressure audit: line-count scan over `scripts/`, `tests/`, `contracts/`, `runtime/` and `agent/`.

## History / provenance

Process history, deleted wrapper paths, prior branch names, dated split closeouts and proof snapshots are no longer maintained in this active inventory. Preserve only durable no-resurrection and migration-gate conclusions here; detailed closeout evidence belongs in `docs/history/**`, OPL series ledgers, machine receipts or git history.
