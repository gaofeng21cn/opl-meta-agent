# opl-meta-agent 私有实现与 OPL 迁移台账

Owner: `opl-meta-agent`
Purpose: `opl_private_implementation_migration_inventory`
State: `active_inventory`
Machine boundary: 本文是 human-readable 迁移治理台账。机器真相继续归 `contracts/`、`runtime/authority_functions/`、scripts 行为、Agent Lab result refs、developer work order refs、candidate refs、owner receipts、target owner receipts / typed blockers、source 和 tests。

## 当前 clean truth

`opl-meta-agent` 是 OPL-compatible Foundry Agent / builder agent，不是 OPL Framework 内置模块。OPL Framework 持有 standard scaffold、Agent Lab、generated interface bundle、runtime、queue、attempt ledger、provider receipt、observability、registry、App workbench、work-order execute/absorb/cleanup 和 promotion gate。

`opl-meta-agent` 只保留 agent-building semantic pack、refs-only action outputs、candidate package / work-order semantic request / mechanism proposal 和 minimal authority functions。Purpose-first owner-delta gate 要求这些输出先回答 target-agent 语义增量是什么、下一 owner 欠什么 delta / receipt / gate / blocker；没有明确 owner route、next forced delta、verification/no-forbidden-write 或 target owner receipt/typed blocker 时，只能返回 typed blocker。Canonical work-order materialization与生命周期归 OPL Foundry Lab；Stage 语义路由由 decisive Attempt 给出，OPL StageRunController 只物化 transition；本仓不保留 current-pointer、transition 或第二路由控制面。

本文只维护当前私有实现分类、active caller、保留 authority、可上收 OPL 子域和退役门槛；不保存分支名、逐日拆分过程、执行流水、proof snapshots 或完整 per-file closeout。退役门槛的机器入口是 `runtime/authority_functions/meta-agent-authority-functions.json#script_morphology_policy/script_to_pack_retirement_gates` 与 source-purity tests；本文解释 gate 语义，不替代机器合同。已移出的过程叙事只通过 [过程历史索引](../history/process/README.md) 读取。

## Machine evidence entrypoints

本文不冻结 live run output、receipt counts、branch/worktree closeout、line-count snapshot 或 dated proof。需要确认当前状态时，从以下入口 fresh-read：

- Source shape and no-resurrection boundary：`contracts/functional_privatization_audit.json`。
- Source-purity and script-to-pack gates：`runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt` / `#script_morphology_policy`、`contracts/script_to_pack_gate_receipt.json`、`contracts/source_structure_policy.json#script_to_pack_receipt_guard`、`contracts/private_functional_surface_policy.json#allowed_opl_surface_consumption_refs`、source-purity tests 和 `npm run source-structure:json`。唯一 readback 同时校验 active caller、source-ref integrity、retention summary、generic materializer no-resurrection、self-guard exclusion 和 allowed OPL surface consumption；它不授权 physical delete、script retirement、OPL primitive parity、readiness、typed blocker instance 或 owner receipt body write。authority truth 只编辑 source parts，再运行 `npm run authority-functions:write` / `npm run authority-functions:check`。
- Generated/default caller consumption：OPL `agents interfaces --repo-dir <this-repo> --json`。
- Production-consumption refs-only ledger：OPL `runtime oma-production-consumption list --json`。
- Active script caller surface：`package.json#scripts`、TypeScript imports, shell invocations and test refs outside `tests/source-purity.test.ts` self-guard strings.
- Split-pressure audit：fresh line-count scan over `scripts/`, `tests/`, `contracts/`, `runtime/` and `agent/`.
- Rational refactor scope audit：first match the requested write-scope paths against tracked files. If the requested Python source/test paths are absent and only analogous TypeScript / JSON surfaces exist, treat the lane as `no_safe_semantic_split` until the write scope is explicitly realigned; do not infer permission to edit adjacent TS/JSON files.

这些入口只能证明 source shape、refs-only consumption、active caller 和 split-pressure。它们不授权 OMA 持有 generated/default shell、target truth、memory/artifact body、owner receipt、domain ready、production ready、quality/export verdict、default promotion、private runner、promotion engine、workbench、scaffold generator、queue、attempt ledger、worktree lifecycle owner、target owner closeout hook owner 或 App/registry owner。

当前脚本治理口径：

`agent/professional_skills/oma-work-order-hygiene/SKILL.md` 是该治理面的 AI-first reviewer；旧 `oma-script-to-pack-hygiene-reviewer` 只保留 redirect。该 workflow-level skill 读取 source-structure readback、source-purity tests、本文、script morphology policy 和 active caller evidence，输出 retain / absorb / delete / tombstone / route-back / work-order recommendation。它只提供专业判断和推荐分类，不授权 physical delete、owner receipt、typed blocker instance、OPL primitive parity、App/generated readiness、default promotion、target/domain/production readiness 或 script retirement。

| Risk surface | Current reading | Gate |
| --- | --- | --- |
| `agent:evidence` / `improve:external-suite` | `agent:evidence` 只生成 evaluation semantic request、canonical work-order materialization request 和 candidate refs；`improve:external-suite` 只消费 target-owned explicit suite result并生成 capability judgment、developer-patch semantic request 或 expected blocker ref。 | 两者都不得分配 work-order identity、调用 Agent Lab suite-plan compiler/runner、写 result/receipt/ledger/blocker body、直接 patch target 或持有 lease/worktree/lifecycle/promotion/owner-closeout authority；target improvement policy 只消费 target-owned change refs / mappings。 |
| `build-agent-baseline` | Production action 已由 OPL hosted `stage_binding` 承接；repo-local developer proof 只消费 `stage-decomposition` / `agent-skeleton-build` 的 OMA domain packet，并构造 OPL scaffold/proof request。 | 不能读取 raw StageRun query、计算 route/current pointer、spawn OPL/Codex、恢复 local stage attempt/Agent Lab runner、result/receipt/ledger、typed-closeout CLI 旁路、implicit fixture graph、fallback route 或 target pack authority。 |
| `stage-decomposition` helpers | `scripts/lib/action-stage-route.ts` 已物理退役；active developer callers只 import `stage-decomposition-pack-draft/{builder,validator,materializer,shared}.ts`，校验 plan/body 分离的 OMA typed closeout并构造 OPL-owned scaffold materialization request。 | 不得解释 OPL readback、启动 runtime、写 attempt receipt、恢复 public compatibility path，或扩成 scaffold generator、physical kernel runtime state owner、conformance gate owner、workbench owner、owner promotion path或target worktree lifecycle owner。 |
| `meta-agent-loop*` helpers | 旧 `meta-agent-loop.ts` re-export facade 与 local OPL command transport 已退役；IO、AI reviewer gate与 thin evaluation-request builder 由相邻 helper直接承接。 | 只消费 host-produced proof packet/receipt；不恢复 compatibility facade、OPL/Codex spawn、receipt/ledger builder、suite-plan compiler 或 private runner。 |
| Retired local wrappers | legacy repo-local Codex plugin/Skill wrapper、installer test、runtime sidecar/projection/lifecycle placeholder 和 `bootstrap-domain-packs` 已退役；当前 `plugins/opl-meta-agent/` 只允许作为 primary skill 的 full-copy carrier。 | 不恢复 `.agents/`、`.codex/`、repo-local CLI/MCP/Skill/product-entry/sidecar/status/workbench wrapper 或 compatibility materialization route；`plugins/opl-meta-agent/` 不能扩成 generic wrapper 或第二 package truth。 |

## Classification

| class | 含义 |
| --- | --- |
| `domain_authority_retained` | agent-building 语义、candidate/work-order/proposal materialization refs；不能写目标 domain truth。 |
| `opl_framework_migration_candidate` | 当前脚本内承担的 registry/App/Agent Lab/promotion/runtime/queue/attempt shell 语义，长期归 OPL。 |
| `already_thin_adapter` | smoke/proof/helper/materializer，仍有 repo-local script caller，但已声明 no generic owner；增长后必须继续拆薄、迁入 pack/contracts/authority refs 或上收 OPL primitive。 |
| `strict_materializer_natural_boundary` | 只校验/物化 typed closeout 或 refs-only packet；继续增长时按 target pack draft 的真实责任边界拆分，而不是按 `*-parts` 实现切片拆分。 |

## 当前脚本治理口径

| surface | class | current active caller | 当前实际职责 | 为什么属于该分类 | OMA 必须保留的 authority | 可迁往 OPL 的 generic 子域 | 迁移/退役门槛 | 推荐验证入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `scripts/lib/agent-evidence-materializer.ts` | `already_thin_adapter` | `npm run agent:evidence`、`tests/agent-evidence-materializer.test.ts` | 读取 target production-evidence contracts，生成 evaluation semantics、canonical work-order materialization request、candidate refs 或 expected blocker ref | contract intake 已拆到 `agent-evidence-contracts.ts`；materializer 不分配 work-order identity，不调用 Agent Lab suite-plan compiler/runner，也不写 result/receipt/ledger/blocker body | target-agent production-evidence semantic/materialization-request refs | OPL work-order materialization、Agent Lab suite-plan compiler/execution/result/receipt ledger、promotion gate、target-owner return primitive、App workbench/registry consumption | stable evaluation vocabulary 继续迁入 pack/contract 或 OPL primitive；不得恢复 local runner、receipt writer 或 target lifecycle | `npm test`、`npm run typecheck` |
| `scripts/improve-from-agent-lab-suite.ts` | `already_thin_adapter` | `npm run improve:external-suite`、external-suite tests | 读取 explicit suite + suite result + reviewer evidence，生成 capability judgment、developer-patch semantic request 或 expected blocker ref | 不分配 work-order identity、不调用 Agent Lab、不直接 patch target、不写 target version/owner receipt、typed-blocker body 或 hosted ledgers；semantic request 必须显式命中 target-owned capability map / policy | target-agent source patch semantic/capability/expected-blocker refs | OPL work-order materialization、target patch execution、Agent Lab regression、promotion gate、worktree lifecycle / absorb / cleanup、owner closeout | 保持 independent reviewer fail-closed 与 target-generic route；不接管 runner、patch lifecycle 或 target owner hook | `npm test`、`npm run typecheck` |
| `scripts/lib/agent-evidence-contracts.ts` | `already_thin_adapter` | imported by `agent:evidence` | 参数解析、target agent production acceptance 定位、required target contract loading 和 repo-relative ref normalization | 是从 `agent-evidence-takeover.ts` 拆出的 contract intake helper；不运行 Agent Lab、不签 target owner receipt、不输出 work order、不持有 target truth/verdict/artifact/memory body | target-agent contract intake refs | OPL generated/default caller、target-agent handoff discovery、Agent Lab intake primitive | 保持 contract-loading helper；不要扩成 generic registry/discovery owner、status reader 或 workbench shell | `npm test`、`npm run typecheck` |
| `scripts/lib/external-suite-materializer.ts` | `already_thin_adapter` | imported by `improve:external-suite` | developer patch work-order、machine closeout refs、runtime/workspace verification refs、no-forbidden-write refs 和 external-suite candidate artifact 写出 | refs-only materializer 不运行 suite、不签 receipt、不写 blocker body、不持有 target truth/verdict/artifact/memory；缺 inputs 时由 caller只返回 expected blocker ref | target-agent external-suite work-order materialization refs | OPL work-order readiness/execution、target-owner return、promotion、worktree lifecycle 和 App/workbench read model | stable vocabulary 进入 `agent/`、`contracts/` 或 OPL primitive；helper 不扩成 runner / queue / gate / lifecycle owner | `npm test`、`npm run typecheck` |
| `scripts/build-agent-baseline.ts` | `developer_proof_request_adapter` | `npm run build-agent-baseline`、`tests/bootstrap-loop.test.ts`；不可从 action/handler/generated default entry到达 | 消费两个 OMA domain closeout packet，构造 scaffold/proof request，并只接受 OPL host-produced materialization/validation receipt | Production action 已是 hosted `stage_binding`；developer helper 不解释 route/readback、不 spawn OPL/Codex、不决定 target stage graph、不签 OPL/owner receipt | candidate Agent Pack semantics、evaluation handoff、stage-decomposition quality-debt refs | OPL StageRun、profile selection、scaffold materialization/validation、generated interfaces、package validation、profile conformance、Agent Lab lifecycle | 保持 request/receipt-only；不得恢复 local command transport、implicit fixture graph、private runner、fake delivery gate 或 compatibility facade | `npm run typecheck`、`npm test`、Framework source-closure |
| `scripts/lib/standard-agent-developer-proof.ts` | `request_producer_and_receipt_consumer` | imported by `build-agent-baseline` and developer-proof tests | 生成固定五项 Framework operation 的 aggregate request，并严格消费 request-SHA、target identity 与 exact input refs 绑定的 aggregate receipt | 不执行 operation、不 spawn OPL/Codex、不写 target files、不把 request 当 materialization/validation evidence；错误 receipt fail closed | `opl_standard_agent_developer_proof_request`、validated proof outputs或 typed contract mismatch | OPL host operation execution、aggregate receipt producer与 package/profile currentness | 保持单一 request/receipt ABI；不得恢复 per-command transport、individual readback acceptance或 fallback execution | `tests/standard-agent-developer-proof.test.ts`、Framework host contract tests、source-purity |
| `scripts/lib/domain-pack.ts` | `domain_semantics_pure_builder` | imports from baseline、takeover、external-suite、work-order helpers and tests | 读取当前 `agent/` domain pack summary；构造 OMA-owned profile/design receipts、primary skill、capability projection 和 Agent Package manifest payload | 只返回内存对象/文本，不创建 target dir、不写 target files、不拥有 pack compiler、generated interface、scaffold IO、digest 或 final build receipt | domain pack summary、profile/design receipt、primary skill、capability/package candidate refs | OPL pack compiler / standard scaffold materializer / generated interface bundle owner | 保持 pure builder；无 active caller 且 tombstone/provenance 具备时才退役 | source-purity tests、`tests/bootstrap-loop.test.ts`、`npm test` |
| `scripts/lib/stage-decomposition-pack-draft/` | `domain_validator_and_opl_scaffold_request_adapter` | imported directly by `build-agent-baseline` and stage-decomposition tests | typed closeout fixture builder、pack draft validator、artifact morphology/stage-native refs builder与 OPL scaffold materialization request builder | OMA 校验 Codex closeout 的 stage graph/action refs/files/gates并产出 domain-authored request；不创建 target dir、不写 target-agent contracts/stages、不计算 materialized digests、不签 final build receipt、不启动 runtime、不消费 scaffold receipt、不 owner promotion、不管理 worktree | stage-decomposition validation refs、`opl_agent_scaffold_materialization_request`、expected receipt/build-receipt refs | OPL scaffold path safety / physical materialization / allowlist merge / pack compiler fold-in / digests / final build receipt | 保留 OMA typed validator/builder；只有 generated OPL action 直接消费同一 OMA schema且 no-active-caller/tombstone 完成后再退役 request adapter | `npm run typecheck`、`tests/stage-decomposition-materializer.test.ts`、`tests/bootstrap-loop.test.ts` |
| `scripts/lib/stage-native-artifact-contract.ts` | `contract_consuming_refs_only_materializer` | imported by stage-decomposition builder/validator and stage-decomposition tests | builds Stage-Native Artifact Contract refs and refs-only attempt template contracts from `contracts/stage_native_artifact_vocabulary.json` | 只生成 artifact/native stage folder, manifest, receipt, blocker, pointer, canonical/export/lineage/retention, physical kernel locator, conformance and workbench consumption refs；稳定 vocabulary、required fields、required files、output roles 和 false-authority flags 归 contract；不读写 runtime state、target artifact body、owner promotion、target worktree or receipt body | stage-native artifact contract and ref template vocabulary contract consumer refs | OPL stage artifact runtime contract compiler / physical kernel locator / conformance / workbench projection primitive | Keep pure refs builder; retire or absorb only after OPL compiler parity and no active caller evidence | `contracts/stage_native_artifact_vocabulary.json`、`tests/stage-decomposition-materializer.test.ts` |
| `scripts/lib/action-stage-route.ts` | `physically_absent_retired_tail` | no active caller | 旧 OMA-local StageRun query/readback evaluator adapter | OPL hosted action runtime 已拥有 route/readback；保留该 adapter 会形成第二语义控制面 | none | OPL StageRun launch/query/read model、route validation与transition materialization | 不得恢复 source、compatibility export、raw readback参数或测试 fixture caller | `rg`、Framework source-closure |
| `scripts/lib/meta-agent-loop-io.ts` | `already_thin_adapter` | direct imports from domain/developer-proof/materializer scripts | stable id、JSON read/write与 target agent descriptor read | local OPL command transport 已退役；helper 不实现 Agent Lab runner、runtime、route/readback、proof receipt validation或executor invocation | target-agent descriptor与repo-local JSON helper refs | OPL-owned hosted action / developer-proof API | 保持无 process spawn、无 generic transport；proof receipt consumer只保留在 dedicated helper | `npm test`、`npm run typecheck`、Framework source-closure |
| `scripts/lib/meta-agent-loop-ai-reviewer.ts` | `already_thin_adapter` with thinner migration input | direct imports from action/materializer scripts | AI reviewer evaluation type、validation、load、receipt fields、acceptance gates | reviewer gate shape 从退役聚合 helper 拆出；只做 fail-closed validation 和 receipt field projection，不签 quality verdict | AI reviewer evidence/gate projection refs | OPL Agent Lab reviewer pool / review-attempt receipt read model | stable reviewer vocabulary 继续迁入 `agent/`、`contracts/` 或 Agent Lab primitive；不授权 promotion | `npm test`、`npm run typecheck` |
| `scripts/lib/foundry-evaluation-semantic-request.ts` | `already_thin_adapter` with authority implementation refs | direct imports from baseline / takeover / agent-evidence scripts | Foundry evaluation semantic-request builder | helper 只生成 domain-owned task intent、quality/improvement/promotion refs；不写 work-order identity、target environment、probe、scorecard/completion policy、observations、suite plan、result、receipt、learning/scaleout ledger 或 promotion state | evaluation semantic-request helper | OPL work-order materialization、Foundry Lab suite-plan compiler/execution/result/receipt ledger、runtime/queue/attempt ledger、promotion gate | keep narrow; stable hosted vocabulary belongs OPL; do not restore receipt/ledger builders, suite-plan compiler or `scripts/lib/meta-agent-loop.ts` facade | `npm test`、`npm run typecheck` |
| `scripts/takeover-agent.ts` | `already_thin_adapter` / `fixture_or_proof_helper` | `npm run takeover:test`、`tests/takeover-loop.test.ts` | 读取 target descriptor、消费 AI reviewer evidence，在内存中生成 takeover semantics、canonical work-order materialization request 和 proposal-only candidate refs | 不分配 work-order identity，不调用 Agent Lab suite-plan compiler/runner，也不写 suite result、takeover/owner receipt、learning ledger、promotion result 或 `new_agent_delivery_gate` readback；source-closure 不得为该 helper 声明 filesystem write | evaluation semantic/materialization-request refs、gated self-evolution/mechanism candidate refs | OPL work-order materialization、Agent Lab suite-plan compiler/runner/result/receipt ledger、promotion gate、target owner closeout、target worktree lifecycle | 保持 smoke/helper；不得恢复 `--fixture` / `--opl-bin` alias、local runner、receipt writer、source-effect 授权或 action-level fake delivery gate | `tests/takeover-loop.test.ts`、`tests/standard-agent-v2-boundary.test.ts`、Framework source-closure、`npm run typecheck` |
| `scripts/lib/work-order-{refs,efficiency}.ts`、`scripts/lib/work-order-materialization-request.ts` | `already_thin_adapter_with_policy_contract_consumer` | imported by baseline / takeover / agent-evidence / external-suite materializers | 共享 target patch-loop refs、runtime/read-model proof、workspace proof、generic refs intake / owner-route normalization / evidence refs extraction、efficiency projection，并封装 canonical materialization request | builder/validation 私有 mechanics 已退役；`work-order-materialization-request.ts` 只组装 OMA semantics，不分配 ID、lease、worktree、lifecycle 或 receipt；`work-order-refs.ts` 只消费 policy contract | semantic request refs、canonical materialization request、refs-only proof builders | OPL work-order identity/materialization/admission/execute/receipt、Agent Lab/App/registry runtime consumption receipts和 promotion gate | 保持 pure helper；不得恢复 builders/validation 私有控制面、runner / queue / promotion gate | `npm test`、`npm run typecheck` |
| `scripts/lib/target-improvement-policy.ts` | `already_thin_adapter_with_policy_contract_consumer` | imported by `improve:external-suite` | 从 target `contracts/capability_map.json`、handoff / production acceptance 显式 change-ref policy 读取 source patch target，生成 proposed change refs、patch traceability matrix、editable surface refs 和 target file hints；通用 owner-receipt / package / typed-blocker 关键词只保留 retired tombstone，不再生成 patch target | 是 refs-only target improvement materializer / contract consumer，不运行 suite、不签 owner receipt、不持有 target verdict；旧内置 generic `external_agent/*` patch-ref fallback 已退役，稳定默认映射也已退役，不能在缺 target-owned policy 时生成 source patch work order | target-agent improvement candidate 的 refs-only mapping / traceability materializer、target-owned capability_map / explicit policy consumer 和 missing-policy typed blocker refs | OPL work-order policy primitive、patch traceability read model、promotion gate | stable vocabulary 继续进入 `contracts/` 或 OPL Agent Lab primitive；helper 不扩成 runner / queue / gate；不得恢复 generic external-agent patch-ref fallback | `npm test`、`npm run typecheck` |
| `contracts/app_workbench_projection.json` | `already_thin_adapter` | contracts tests, OPL App consumption refs | refs/status/receipt/blocker projection contract | 已声明 App/workbench owner is OPL; OMA only supplies refs；不是 repo-owned workbench 或 compatibility facade | OMA work-order/candidate refs | App/workbench display shell | keep refs-only; live App screenshot/evidence from OPL/App side | `tests/contracts-generated-surfaces.test.ts`、`tests/external-suite-patch-loop-projection.test.ts` |
| `contracts/opl_domain_manifest_registration.json` | `already_thin_adapter` | contracts tests, OPL registry/discovery refs | registry discovery metadata | registry owner is OPL; OMA supplies registration refs only | domain descriptor/handoff refs | registry/discovery owner | OPL registry receipt required before live claim | `tests/contracts-generated-surfaces.test.ts` |
| `takeover:test --fixture` alias | `physically_absent_retired_tail` | no active caller; no parser branch after retirement | 旧 takeover parser 兼容 alias | 已由 `--agent-dir` 显式 target intake 替代；`--fixture` 现在只走普通 unknown-argument fail-closed 路径，不再保留专门 alias 语义 | none | none | 不得恢复 `--fixture` parser branch、public caller 或 compatibility alias | `tests/takeover-loop.test.ts` |
| `runtime/authority_functions/meta-agent-authority-functions.json` | `domain_authority_retained` generated aggregate | contracts tests, authority function refs, source-structure strict gate | explicit authority refs for package builder and proposal authorizer; generated from `.source.json` / `.leaf-index.json` / `meta-agent-authority-functions.parts/**` | minimal refs-only authority functions belong OMA, but cannot own runtime; aggregate path remains for consumers while source parts carry maintenance truth | candidate package builder, proposal authorizer refs | none except OPL invocation shell | keep no generic owner flags; scripts must match implementation refs; edit source parts and run `npm run authority-functions:check` before claiming no drift | `tests/contracts-action-authority.test.ts`、`tests/contracts-generated-surfaces.test.ts`、`npm run authority-functions:check` |

## Inventory by owner boundary

| Boundary | Active surfaces | OMA 保留 authority | OPL / target owner authority |
| --- | --- | --- | --- |
| Candidate baseline materialization | OPL hosted `build-agent-baseline` stage binding、OMA domain packets、stage-decomposition request adapter、domain-pack pure builders | candidate Agent Pack semantics、thin evaluation request、Foundry evaluation-work-order candidate、OMA-owned quality-debt refs | OPL StageRun launch/query/route owner、profile selection、standard scaffold target IO、materialized digest/final build receipt、generated interface、package/profile conformance、Agent Lab lifecycle、target owner closeout、promotion gate |
| External evidence / suite improvement | `scripts/lib/agent-evidence-materializer.ts`、`scripts/improve-from-agent-lab-suite.ts`、materializers、target-improvement helpers | thin evaluation request、Foundry evaluation/developer work order、target capability/mechanism candidate、expected blocker refs | Agent Lab suite-plan compiler/execution/result/receipt ledger、target patch lifecycle、truth/quality/artifact/memory/owner receipt/blocker body、promotion gate |
| External work-order execution | OMA repo-local execution wrapper/action/schema 已物理退役；work-order builders只输出 semantic request | `opl work-order materialize-request` / `opl work-order execute`、absorb / cleanup、target owner closeout hook、execution receipt |
| Generated / registry / App projection | `contracts/generated_surface_handoff.json`、`contracts/opl_domain_manifest_registration.json`、`contracts/app_workbench_projection.json` | domain descriptor、action/stage refs、work-order/candidate refs | OPL generated interfaces、registry/discovery owner、App/workbench shell/live rendering |
| Authority functions | `runtime/authority_functions/meta-agent-authority-functions.json`, `runtime/authority_functions/meta-agent-authority-functions.parts/**` and matching scripts | minimal package builder and proposal authorizer refs | generic runtime, queue, scheduler, observability, promotion gate |

## Retired legacy surfaces

本 active inventory 不再维护逐项退役 surface 表。当前读法是：机器缺席 / no-resurrection truth 归 `runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt`、`script_morphology_policy.script_to_pack_retirement_gates` 和 source-purity tests；人读退役 provenance 归 [`docs/history/process/retired-surface-provenance.md`](../history/process/retired-surface-provenance.md)。

Active 语境只保留当前迁移门：legacy repo-local Codex plugin / Skill wrapper、runtime sidecar/status/workbench shell、direct graph / implicit fixture path、compatibility facade、aggregate re-export facade、generic `external_agent/*` patch-ref fallback 等已退役面不得恢复为 active caller、fallback、alias、facade、wrapper、test-only convenience 或 current owner。当前 `plugins/opl-meta-agent/` 只允许作为 `agent/primary_skill/SKILL.md` 的 materialized carrier。若未来要物理删除仍有 active caller 的脚本、helper、contract 或 workflow，必须先满足对应 machine gate 的 OPL primitive parity、no-active-caller、no-forbidden-write、owner receipt / typed blocker 或 explicit owner decision，以及 tombstone/provenance refs。

## Current migration gates

| Gate | Current handling |
| --- | --- |
| owner-local cleanup candidate readback | Cleanup readback is diagnostic only. Retained scripts with active package-script, import, shell, or test callers stay as materializer/helper/verification-wrapper rows until their gate shows OPL primitive parity or explicit fixture/proof refs, no-active-caller, no-forbidden-write where applicable, and tombstone/provenance evidence. Current readback does not authorize owner-local physical delete. |
| Source-structure command aliases | `line-budget` and `line-budget:strict` npm/shell aliases are retired. Canonical commands are `source-structure` and `source-structure:strict`; `contracts/source_structure_policy.json#compatibility_aliases` is empty and `npm run source-structure:json` must not declare `compatibility_alias_retained`. |
| Test support stdlib facade | Tests import `node:assert/strict`, `node:fs`, `node:os`, `node:path` and `node:child_process` directly. `tests/support/contracts.ts` only keeps repo-specific helpers, shared constants and JSON/file helpers; `tests/support/external-suite-fixtures.ts` keeps only external-suite fixture builders/assertions and reuses `writeJsonFile`. |
| Legacy professional skill redirects | Physical `agent/professional_skills/*/TOMBSTONE.md` redirect files are retired. Legacy redirect identity lives in `contracts/capability_map.json#legacy_professional_skill_redirects` as `legacy-professional-skill:<id>` plus the human README ledger; these entries are not repo paths and carry no independent authority. |
| external work-order delegation retirement | OMA repo-local execution wrapper, package script, generated action, schemas and tests are absent. OPL validates/materializes/executes the typed request and owns target lifecycle/readback; the target owner retains owner receipt body authority. No OMA delegation envelope may be restored. |
| agent evidence / external suite semantic request retention | The `agent_evidence_and_external_suite_materializers` source group retains OMA judgment and typed request construction only. It emits embedded evaluation/work-order/candidate bodies and expected blocker refs, with `oma_writes_request_files=false`; it does not create directories, write bundles, hash files, execute work orders, validate execution receipts, or own lifecycle/ledgers. |
| Work-order policy and efficiency refs | Keep small `scripts/lib/work-order-*` helpers as refs-only builders/validators while stable vocabulary moves to pack/contracts/OPL primitive. Do not recreate a monolithic private work-order policy owner. |
| Target improvement and agent-evidence request construction | Keep `target-improvement-policy.ts`, `agent-evidence-materializer.ts` and `external-suite-materializer.ts` as semantic builders only. Target improvement default owner-receipt/live-acceptance mappings are retired tombstones under `contracts/developer_work_order_policy.json#target_improvement_default_change_ref_policy`; helpers must consume target-owned `capability_map` or explicit policy before issuing source patch refs. They cannot materialize files, run Agent Lab, sign owner receipts, write target truth, own promotion gates, become App/workbench read models, or invent generic patch refs when the target agent has not declared explicit change-ref policy. |
| Baseline generation | `build-agent-baseline` remains a smoke/action implementation that consumes explicit domain closeouts plus OPL scaffold/interface projection, then emits AgentBuildReceipt、evaluation semantics 和 canonical work-order materialization request. It cannot regain implicit fixture/default graph authority, work-order identity/lifecycle mechanics, or local Agent Lab suite-plan/result/receipt/ledger authority. |
| Stage-decomposition pack-draft natural boundary | `scripts/lib/stage-decomposition-pack-draft.ts` barrel re-export is retired; active callers import `scripts/lib/stage-decomposition-pack-draft/` directly. `scripts/lib/domain-pack.ts` remains only a pack summary / target-agent fixture helper and inherits the same pack compiler / generated interface owner / target pack authority no-resurrection guard. Future work should reduce retained helper semantics only after OPL primitive parity and no-active-caller proof; do not restore a barrel facade or re-merge builder / validator / materializer into one private script. |
| Meta-agent loop helper tail | Keep direct IO / reviewer / receipt helpers as current surfaces. The old `scripts/lib/meta-agent-loop.ts` re-export facade is retired and must not return as a compatibility import path. Any stable reviewer gate or proposal policy should move to contracts/pack/OPL primitive rather than into another aggregate helper. |
| Repo shell verification wrappers | Keep `scripts/repo-hygiene.sh`, `scripts/run-with-repo-temp-env.sh` and `scripts/verify.sh` only as repo-native verification / hygiene / external-cache-boundary wrappers. `run-with-repo-temp-env.sh` delegates the read-only Framework-link prerequisite to the canonical OPL Packages `packages link-framework --check` surface, propagates its typed failure / repair command once at the outer boundary, and lets nested npm scripts reuse the active temp environment; it does not materialize links or implement a resolver. These scripts are tracked by the same machine script-to-pack gate as TypeScript materializers and cannot become generic CLI wrappers, runtime owners, schedulers, queues, generated interface owners, target truth writers, or default hard line-budget gates. |
| Authority manifest source split | Keep `scripts/sync-authority-functions.ts` only as generated aggregate maintenance helper. `runtime/authority_functions/meta-agent-authority-functions.json` remains the stable consumer surface, but strict source-structure truth lives in `.source.json`, `.leaf-index.json`, `.bundle-manifest.json` and `meta-agent-authority-functions.parts/**`. This helper cannot become generated interface owner, registry owner, runtime owner or authority-function truth owner. |
| Authority aggregate sync helper | `scripts/lib/sync-json-bundle.ts` is the thin JSON file IO, stable text, digest and CLI dispatch helper consumed by `scripts/sync-authority-functions.ts`. Authority refs, leaf structure, source digest inputs and bundle semantics stay in that entrypoint; the helper cannot become a generic framework, generated interface owner, runtime owner or domain truth owner. |
| Wrapper no-resurrection | Do not add repo-local scheduler, queue, attempt ledger, CLI/MCP/Skill/product-entry wrapper, status/workbench shell, registry owner, Agent Lab runner, promotion gate, compatibility facade, or domain-specific command family. |
| Script-to-pack retirement gates | Use `contracts/script_to_pack_gate_receipt.json`, the authority morphology/source-purity surfaces, `npm run source-structure:json` and source-purity tests before any absorb or retire claim. Every scanned `.ts` or `.sh` script must have a real non-self caller, repo-local source ref and retention/retirement classification. The retention summary is a structure check only; it cannot authorize delete, typed blocker instances, owner receipts, OPL primitive parity, readiness or promotion. |

## Cleanup parity retention boundary

当前 retention 结论来自 `npm run source-structure:json` 的分类摘要，不是 `ready`、
`parity_complete` 或物理删除授权。strict line-budget 与 retention classification 是同一
readback 中的不同维度，不再维护独立 compact/full cleanup surfaces。

| Cleanup item | Current gate / readback route | Result | Owner route / required primitive | Tombstone stop condition |
| --- | --- | --- | --- | --- |
| `6` `scripts/execute-external-work-order.ts` | OPL owns work-order materialization/admission/execute/dry-run/absorb/cleanup/owner-closeout readback；OMA wrapper、package/action/schema/test caller均已删除。 | `retired` | `opl work-order materialize-request` + `opl work-order execute` | No-resurrection provenance recorded；source-purity receipt no longer scans the path. |
| `7` `stage-decomposition-pack-draft/**` and `work-order-*` helpers | Generic physical materialization and work-order lifecycle are delegated: `opl agents scaffold --materialize-request` owns target IO/digests/final build receipt, and `opl work-order execute` owns worktree execute/absorb/cleanup/owner-closeout readback. OMA retains typed stage-decomposition judgment and developer-work-order schema/refs validation. | `generic_mechanics_delegated_domain_validators_retained` | Keep OMA domain builders/validators thin; OPL generated actions may later consume their schemas directly. | No active import/test/package caller; equivalent generated-action schema validation; source-purity receipt refresh; tombstone/provenance ref. |

Fixture/test support boundary: `tests/source-purity/script-morphology.test-case.ts` remains intentionally explicit because it guards machine receipt and policy fields. Test-side standard-library re-export facades and duplicate external-suite JSON/repo helpers should stay retired; `tests/support/external-suite-fixtures.ts` reuses the repo-local JSON helper instead of carrying duplicate mkdir/write/return code. This does not change item `6/7` retention state or authorize any retained script deletion.

## OPL primitive dependencies

- Agent Lab suite runner and evolution/promotion gate.
- Generated interface bundle and standard scaffold generator.
- Pack compiler default domain-pack materializer for bootstrap / real-target smoke specs.
- Registry/discovery and App/workbench drilldown live consumption.
- Stage attempt ledger, queue, provider receipt and observability.
- Target-agent generic handoff vocabulary across MAS/MAG/RCA and new Foundry Agents.

## Forbidden claims

- `opl-meta-agent` work order does not sign target owner receipt.
- Suite pass, schema completeness, generated surface readiness or mechanism proposal cannot promote default agent.
- OMA cannot write target domain truth, memory body, artifact body, quality/export verdict or owner receipt authority.
- Registration/App projection readiness is not live App rendering evidence.

## Dynamic evidence entrypoints

- Source-shape and functional privatization: `contracts/functional_privatization_audit.json`.
- Source-purity / script-to-pack dynamic entrypoints: `contracts/script_to_pack_gate_receipt.json`、authority source-purity / morphology surfaces、`npm run source-structure:json` and source-purity tests. These surfaces own active-caller, source-ref, retention-summary, generic-materializer and self-guard details；本文只保持迁移分类和 non-claim rule。
- Generated/default caller consumption: OPL `agents interfaces --repo-dir <this-repo> --json`.
- OMA production-consumption refs-only ledger: OPL `runtime oma-production-consumption list --json`.
- Script caller surface: `package.json#scripts`, `tests/*.test.ts` and TypeScript imports.
- Large-file/split-pressure audit: line-count scan over `scripts/`, `tests/`, `contracts/`, `runtime/` and `agent/`.

## History / provenance

Process history, deleted wrapper paths, prior branch names, dated split closeouts and proof snapshots are no longer maintained in this active inventory. Preserve only durable no-resurrection and migration-gate conclusions here; detailed closeout evidence belongs in `docs/history/**`, OPL series ledgers, machine receipts or git history.
