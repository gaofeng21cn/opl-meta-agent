# opl-meta-agent 私有实现与 OPL 迁移台账

Owner: `opl-meta-agent`
Purpose: `opl_private_implementation_migration_inventory`
State: `active_inventory`
Machine boundary: 本文是 human-readable 迁移治理台账。机器真相继续归 `contracts/`、`runtime/authority_functions/`、scripts 行为、Agent Lab result refs、developer work order refs、candidate refs、owner receipts 和目标 agent owner receipts。
Date: `2026-05-23`

## 当前 clean truth

`opl-meta-agent` 是 OPL-compatible Foundry Agent / builder agent，不是 OPL Framework 内置模块。OPL Framework 持有 standard scaffold、Agent Lab、generated interface bundle、runtime、queue、attempt ledger、provider receipt、observability、registry、App workbench 和 promotion gate。`opl-meta-agent` 只保留 agent-building semantic pack、refs-only action outputs、candidate package / developer work-order / mechanism proposal materialization 和 minimal authority functions。2026-05-25 active source shape 的 machine signal 是 `functional_structure_gap_count=0`，表示本仓不保留 repo-owned generic runtime、generated shell、workbench、sidecar 或 compatibility surface；它不表示所有 scripts/materializers 已达到最终纯净形态。Default caller / sidecar / workbench 只作为 OPL-owned generated/default surface refs 与 OMA boundary evidence tail 出现；scripts 必须持续保持 materializer/helper 角色，并把稳定 policy 迁回 `agent/`、`contracts/`、`runtime/authority_functions/` 或 OPL primitive。

本轮 fresh scan 发现 repo-tracked TS/Python/CLI/status/runtime/watch/workbench/product-entry/projection/locator/storage/attempt-ledger/state-machine 实现文件没有超过 800 行；超过 1000 行的文件集中在测试聚合面：`tests/external-suite-self-evolution.test.ts` 1450 行、`tests/contracts.test.ts` 1351 行。source-purity 断言已从 `tests/contracts.test.ts` 拆出为 `tests/source-purity.test.ts` 215 行，避免 strict source-purity 治理继续堆回超长 contract 聚合测试。实现侧最大的文件是 `scripts/lib/agent-evidence-materializer.ts` 594 行，其次是 `scripts/agent-evidence-takeover.ts` 516 行、`scripts/lib/meta-agent-loop-receipts.ts` 497 行、`scripts/bootstrap-sample-agent.ts` 484 行、`scripts/improve-from-agent-lab-suite.ts` 479 行。当前没有发现安全、低风险且自然的大型私有控制面代码拆分切口；继续机械拆分会降低 refs-only materializer 的局部可读性。

本轮 scan 的平台化风险面仍集中在 `scripts/agent-evidence-takeover.ts`、`scripts/improve-from-agent-lab-suite.ts`、`scripts/bootstrap-sample-agent.ts`、`scripts/lib/agent-evidence-materializer.ts` 和 `scripts/lib/meta-agent-loop-receipts.ts`。这些脚本当前可用，但容易继续吸收 policy 并变成私有 runner / promotion engine / workbench。台账口径是：脚本只能作为 authority implementation、smoke helper、fixture/proof helper 或 developer work-order materializer；可声明的策略继续迁回 `agent/`、`contracts/` 或 `runtime/authority_functions/`。

2026-05-21 本轮最小清理先把 `agent:evidence` 与 `improve:external-suite` 共享的 target patch-loop machine refs、runtime/read-model consumption proof、target workspace environment proof、no-patch/source-patch closeout evidence、默认 forbidden target surfaces、work-order readiness / executor aperture / target verification / owner route / no-forbidden-write completeness、developer patch work-order shape validator、generic refs intake 和 OPL work-order primitive refs 集中到 `scripts/lib/work-order-policy.ts`，让两个入口脚本只输出 `required_opl_work_order_primitive_refs` 并保持 refs-only materializer 边界。2026-05-22 `codex/oma-work-order-policy-natural-split` 已把该 717 行单文件继续拆成 `work-order-policy-constants.ts`、`work-order-refs.ts`、`work-order-efficiency.ts`、`work-order-builders.ts` 和 `work-order-validation.ts`，分别承载常量、refs 解析、efficiency refs 收集、work-order/readiness builders 与 fail-closed shape validation；调用方现在直接 import 语义模块，不再经由大型聚合 helper。本轮 `codex/oma-workorder-policy-thinning` 又把 external-suite improvement 的 target improvement policy、change-ref mapping、patch traceability matrix、editable surface inference 和 target file-hint lookup 从 `scripts/improve-from-agent-lab-suite.ts` 抽到 `scripts/lib/target-improvement-policy.ts`，使入口脚本降为 external suite consumption 与 candidate/work-order/proposal materializer。随后 `codex/oma-agent-evidence-materializer-thinning` 把 `agent:evidence` 内的 owner receipt refs、target capability candidate、developer work order、mechanism proposal 和 typed blocker 输出组装迁入 `scripts/lib/agent-evidence-materializer.ts`；`scripts/agent-evidence-takeover.ts` 当前为 516 行入口，继续只负责 target contract intake、suite 生成、OPL Agent Lab 调用和 artifact 落盘。`scripts/agent-evidence-takeover.ts` 和 `scripts/improve-from-agent-lab-suite.ts` 因此更接近纯 materializer，通用 work-order readiness / validation primitive 继续指向 OPL Framework work-order primitive owner。OMA 只消费 refs 并 materialize target-agent evidence intake 后的 work-order / proposal / blocker。OPL replacement 或 live promotion gate 仍未在本仓声明完成。

同轮 family-retire-legacy-oma 清理已删除无 active caller 的 repo-local Codex plugin/Skill wrapper：`plugins/opl-meta-agent/**`、`scripts/install-codex-plugin.ts`、`tests/codex-plugin.test.ts`；同时删除仅承载占位 README 的 `runtime/sidecar/`、`runtime/projection_builders/`、`runtime/lifecycle_adapters/`。当前 `runtime/` 只允许 `authority_functions/`，OPL generated Skill/product-entry、sidecar/projection dispatch、status read model 和 workbench shell 继续由 OPL Framework 生成或托管。

2026-05-24 `codex/oma-standard-agent-physical-20260524` fresh physical scan 确认 tracked tree 中没有 repo-local `plugins/`、`.agents/`、`.codex/`、runtime sidecar/projection/lifecycle adapter、CLI/MCP/product-entry/workbench wrapper，`package.json` 也没有 `bin`、`main` 或 `exports`。`agent/` 的 prompt / skill / knowledge / quality gate / stage refs 全部解析到真实非空 pack 文件，且 `required_domain_pack_paths` 不包含 README。`contracts/pack_compiler_input.json` 的 `minimal_authority_functions` 已收敛为 `candidate_agent_package_builder` 与 `mechanism_patch_proposal_authorizer`，并由 `tests/contracts.test.ts` 锁定为与 `runtime/authority_functions/meta-agent-authority-functions.json` 一致，避免历史 scaffold 名称重新宣称 owner receipt signer、target boundary verdict 或 online-learning reviewer authority。

2026-05-25 `codex/oma-standard-agent-purity-20260525` 将 active docs/contracts 的剩余 default-caller 表达收口为 evidence tail：`contracts/functional_privatization_audit.json` 显式记录 `source_shape=landed`、`functional_structure_gap_count=0`、`domain_repo_retained_generic_surface_count=0` 和 `remaining_tail_kinds=[opl_generated_default_caller_consumption_tail, domain_refs_only_adapter_thinning, script_to_pack_hygiene, evidence_tail]`。`contracts/default_caller_deletion_evidence.json` 只说明 OMA 提供 typed blocker、no-forbidden-write、tombstone/provenance 和 retained domain authority refs，不授权本仓物理删除或长期持有 generated/default shell。后续治理只剩 OPL generated/default caller consumption receipt、refs-only adapter thinning、script-to-pack hygiene 与更多真实 target evidence tail。

2026-05-25 `codex/goal-oma-source-purity` 将 strict source-purity 检查落到 `runtime/authority_functions/meta-agent-authority-functions.json#source_purity_scan_receipt` 与独立 `tests/source-purity.test.ts`。该 receipt 固定扫描范围、全部 tracked TS script refs、缺席的 repo-owned wrapper/runtime 路径、package surface 边界、保留的非污染 authority 面和禁止长期组成声明；测试同步校验 package 无 `bin/main/exports`、`runtime/` 只剩 `authority_functions/`、所有 scripts 都被 script morphology 分类且没有 forbidden roles。该改动不新增 wrapper、不声明 OPL 接管完成，只把 source-purity hygiene 从 prose 约束升级为 machine-readable guard。

2026-05-22 `codex/oma-bootstrap-domain-pack-split` 已把 `scripts/bootstrap-sample-agent.ts` 内的 sample / real-target domain-pack writer 拆到 `scripts/lib/bootstrap-domain-packs.ts`。入口脚本当前为 484 行，继续只负责 bootstrap orchestration、OPL scaffold / Agent Lab 调用、baseline receipt 和 scaleout ledger 落盘；新 helper 只物化 refs-only `action_catalog` 与 `stage_control_plane` 测试包，不声明 OPL standard scaffold/generator 或 Agent Lab primitive 已完成替代。

2026-05-22 `codex/oma-external-suite-materializer` 已把 `scripts/improve-from-agent-lab-suite.ts` 中 developer patch work-order 和 efficiency typed blocker 输出组装拆到 `scripts/lib/external-suite-materializer.ts`。入口脚本当前为 479 行，只保留 external suite 读取、OPL Agent Lab 调用、candidate/proposal/receipt 编排和 artifact 落盘；新 helper 为 314 行 refs-only materializer，不运行 Agent Lab、不签 target owner receipt、不持有 target truth/verdict/artifact/memory body，也不声明 OPL runner、promotion gate 或 App/workbench 已接管。

2026-05-22 `codex/oma-meta-agent-loop-split` 已把 `scripts/lib/meta-agent-loop.ts` 从 754 行聚合 helper 拆成 3 行公开 re-export 入口，并新增 `meta-agent-loop-io.ts`、`meta-agent-loop-ai-reviewer.ts` 与 `meta-agent-loop-receipts.ts`。本次进展只表示 `needs_split_before_migration` 风险面转为 thinner migration input：OPL CLI/JSON/target-agent 读取、AI reviewer gate、suite/receipt/proposal builder 的职责边界更清楚；不声明 OPL 已完全接管，不新增兼容层，不改变 agent-building domain semantics。

## Classification

| class | 含义 |
| --- | --- |
| `domain_authority_retained` | agent-building 语义、candidate/work-order/proposal materialization refs；不能写目标 domain truth。 |
| `opl_framework_migration_candidate` | 当前脚本内承担的 registry/App/Agent Lab/promotion/runtime/queue/attempt shell 语义，长期归 OPL。 |
| `already_thin_adapter` | smoke/proof/helper/materializer，仍有 repo-local script caller，但已声明 no generic owner；不是最终完成态，增长后必须继续拆薄、迁入 pack/contracts/authority refs 或上收 OPL primitive。 |
| `needs_split_before_migration` | 脚本混合 target handoff parsing、policy、work-order generation、Agent Lab invocation 或 proposal assembly，需要先抽出共享 policy / authority helper。 |

## Inventory

| surface | lines | class | current active caller | 当前实际职责 | 为什么属于该分类 | OMA 必须保留的 authority | 可迁往 OPL 的 generic 子域 | 迁移/退役门槛 | 推荐验证入口 |
| --- | ---: | --- | --- | --- | --- | --- | --- | --- | --- |
| `scripts/agent-evidence-takeover.ts` | 516 | `already_thin_adapter` with split pressure | `npm run agent:evidence`、`tests/agent-evidence-takeover.test.ts` | 读取目标 agent production acceptance / handoff，生成 Agent Lab suite，调用 OPL Agent Lab，落盘 developer work order、capability candidate、mechanism proposal 或 typed blocker artifacts | owner receipt refs、capability candidate、developer work order、mechanism proposal 和 typed blocker 输出组装已迁入 `scripts/lib/agent-evidence-materializer.ts`；入口脚本仍调用 OPL Agent Lab 和写 artifact，因此继续保持 split-pressure 观察，不能扩成 generic evidence runner / promotion gate | target-agent production evidence intake 后的 work-order/proposal/blocker refs | Agent Lab suite runner、attempt ledger、promotion gate、work-order readiness primitive、target-owner return primitive、App workbench/registry consumption | stable proposal / blocker vocabulary 继续迁入 pack/contract 或 OPL Agent Lab primitive；script 保持 thin materializer；不得写 target truth / quality / artifact / memory / owner receipt | `npm test`、`npm run typecheck` |
| `scripts/improve-from-agent-lab-suite.ts` | 479 | `already_thin_adapter` with split pressure | `npm run improve:external-suite`、external-suite tests | external suite 读取、OPL Agent Lab 调用、candidate/proposal/receipt 编排和 artifact 落盘 | target improvement policy 已迁入 `target-improvement-policy.ts`，developer patch work-order / efficiency typed blocker materialization 已迁入 `external-suite-materializer.ts`；入口脚本仍调用 OPL Agent Lab 和写 artifact，因此保持 split-pressure 观察，不能扩成 generic evidence runner / promotion gate | target-agent source patch work-order/proposal/blocker refs | Agent Lab runner、promotion gate、target runtime/App read model shell、generic work-order readiness primitive | stable policy text 继续迁入 pack/contract；保持 independent reviewer fail-closed；no domain-specific command family | `npm test`、`npm run typecheck` |
| `scripts/lib/external-suite-materializer.ts` | 314 | `already_thin_adapter` | imported by `improve:external-suite` | developer patch work-order、efficiency typed blocker、machine closeout refs、runtime/workspace verification refs 和 no-forbidden-write refs-only 输出组装 | 是从 `improve-from-agent-lab-suite.ts` 拆出的 refs-only materializer helper；不运行 suite、不签 target owner receipt、不持有 target truth/verdict/artifact/memory body；只把 external-suite candidate 转为 work-order/blocker shape | target-agent external-suite intake 后的 work-order/blocker materialization refs | OPL work-order readiness primitive、target-owner return primitive、promotion gate 和 App/workbench read model | stable vocabulary 进入 `agent/`、`contracts/` 或 OPL Agent Lab primitive；helper 不扩成 runner / queue / gate | `npm test`、`npm run typecheck` |
| `scripts/bootstrap-sample-agent.ts` | 484 | `already_thin_adapter` | `npm run bootstrap:sample`、`tests/bootstrap-loop.test.ts` | sample agent / real-target bootstrap orchestration、baseline receipt、scaleout ledger | sample / real-target domain-pack writer 已迁入 `scripts/lib/bootstrap-domain-packs.ts`；入口脚本仍调用 OPL scaffold 与 Agent Lab 并写 receipt/ledger，因此保持 smoke helper，不得扩成 product runtime、standard scaffold owner 或 private Agent Lab runner | candidate package refs、baseline receipt refs、scaleout evidence refs | standard scaffold/generator, Agent Lab runner, bootstrap read model / ledger projection | OPL default scaffold/generator 与 Agent Lab caller parity 可替代该 smoke path 后，再把脚本降为 fixture 或退役；当前不能写成已接管 | `npm run typecheck`、`npm test`、`npm run verify` |
| `scripts/lib/bootstrap-domain-packs.ts` | 253 | `already_thin_adapter` with migration input | imported by `scripts/bootstrap-sample-agent.ts` | 物化 sample / real-target refs-only `action_catalog` 与 `stage_control_plane` 测试包 | 是从 bootstrap 入口拆出的 contract-pack writer helper；不运行 scaffold、不运行 Agent Lab、不签 receipt、不持有 target truth/verdict/artifact/memory body；仍暴露 OPL 应提供默认 scaffold / pack materializer 的迁移输入 | sample / real-target candidate package contract refs | OPL standard scaffold/generator、pack compiler default domain-pack materializer、generated interface bundle | 当 OPL pack compiler 可从 declarative target spec 生成相同 action/stage refs，并由 bootstrap tests 切到 OPL default caller 后，helper 可退役 | `npm run typecheck`、`npm test`、`npm run verify` |
| `scripts/lib/meta-agent-loop.ts` | 3 | `already_thin_adapter` thin public entry | imported by bootstrap/sample and evidence scripts | public re-export for existing callers | 原调用路径保持稳定，具体职责已移入相邻小模块；不新增兼容层 | public import surface for existing authority helpers | none; generic runtime remains OPL-owned | keep as thin re-export while active callers import this path | `npm test`、`npm run typecheck` |
| `scripts/lib/meta-agent-loop-io.ts` | 70 | `already_thin_adapter` with thinner migration input | re-exported by `meta-agent-loop.ts` | `DEFAULT_OPL_BIN`、OPL CLI JSON call、stable id、JSON read/write、target agent descriptor read | 从聚合 helper 拆出的 IO / OPL client helper；只消费 OPL CLI，不实现 Agent Lab runner 或 runtime | OPL invocation helper refs、target-agent descriptor intake | OPL-owned invocation shell / generated interface caller | retain as small helper until OPL caller parity can replace local script usage | `npm test`、`npm run typecheck` |
| `scripts/lib/meta-agent-loop-ai-reviewer.ts` | 195 | `already_thin_adapter` with thinner migration input | re-exported by `meta-agent-loop.ts` | AI reviewer evaluation type、validation、load、receipt fields、acceptance gates | reviewer gate shape 从聚合 helper 拆出；只做 fail-closed validation 和 receipt field projection，不签 quality verdict | AI reviewer evidence/gate projection refs | OPL Agent Lab reviewer pool / review-attempt receipt read model | stable reviewer vocabulary 继续迁入 `agent/`、`contracts/` 或 Agent Lab primitive；不授权 promotion | `npm test`、`npm run typecheck` |
| `scripts/lib/meta-agent-loop-receipts.ts` | 497 | `already_thin_adapter` with thinner migration input | re-exported by `meta-agent-loop.ts` | Agent Lab suite、owner receipt、learning candidate、mechanism proposal、real-target delivery receipt 与 scaleout ledger builders | suite/receipt/proposal builders 从聚合 helper 拆出；仍只是 refs-only materializer，不运行 Agent Lab、不持有 target truth/verdict/artifact/memory body | package/work-order/proposal materializer helpers | Agent Lab execution, runtime/queue/attempt ledger, promotion gate | continue moving stable vocabulary to pack/contract/OPL primitive; no generic runner ownership | `npm test`、`npm run typecheck` |
| `scripts/lib/work-order-{policy-constants,refs,efficiency,builders,validation}.ts` | 57 / 165 / 146 / 272 / 97 | `already_thin_adapter` | imported by `agent:evidence` and `improve:external-suite` materializers | 共享 target patch-loop closeout refs、runtime/read-model proof、workspace proof、forbidden target surface defaults、refs-only work-order completeness builder、developer patch work-order validator、generic refs intake / owner-route normalization / evidence refs extraction、`*_efficiency_handoff_projection` intake 和 OPL work-order primitive refs builder | 旧 `work-order-policy.ts` 已自然拆分为职责明确的 refs-only helper；不运行 suite、不签 promotion、不持有 target truth；primitive refs 只指向 OPL Framework work-order primitive owner；validator 只做 shape fail-closed，不授权 patch 或 promotion；efficiency handoff 只消费 refs-only projection，不新增 domain-specific command family | work-order policy constants、refs-only proof builders、target-agent evidence materializer helpers、shape validation | OPL Agent Lab/App/registry runtime consumption receipts、generic readiness primitive implementation 和 promotion gate | 保持 pure helper；稳定合同文本继续迁入 `agent/`、`contracts/` 或 authority refs；不得重新合并成 runner / queue / promotion gate | `npm test`、`npm run typecheck` |
| `scripts/lib/agent-evidence-materializer.ts` | 594 | `already_thin_adapter` | imported by `agent:evidence` | owner receipt refs、target capability candidate、developer patch work order、mechanism proposal 和 typed blocker refs-only 输出组装 | 是从 `agent-evidence-takeover.ts` 拆出的 materializer helper；不运行 Agent Lab、不签 target owner receipt、不持有 target truth/verdict/artifact/memory body；仅把 target evidence intake 转为 refs-only work-order/proposal/blocker shape；当前虽是最大实现文件，但仍低于 800 行且函数边界自然 | target-agent evidence intake 后的 work-order/proposal/blocker materialization refs | OPL work-order readiness primitive、target-owner return primitive、promotion gate 与 App/workbench read model | stable vocabulary 进入 `agent/`、`contracts/` 或 OPL Agent Lab primitive；helper 不扩成 runner / queue / gate；若接近 800 行再按 owner receipt refs / capability candidate / work-order / mechanism proposal / typed blocker 分段拆分 | `npm test`、`npm run typecheck` |
| `scripts/lib/target-improvement-policy.ts` | 365 | `already_thin_adapter` | imported by `improve:external-suite` | 从 target handoff / production acceptance 读取 change-ref policy，生成 proposed change refs、patch traceability matrix、editable surface refs 和 target file hints | 是 refs-only target improvement policy helper，不运行 suite、不签 owner receipt、不持有 target verdict；从入口脚本抽出后降低私有 promotion/workbench 聚合风险 | target-agent improvement candidate 的 refs-only mapping / traceability materializer | OPL work-order policy primitive、patch traceability read model、promotion gate | stable vocabulary 进入 `contracts/` 或 OPL Agent Lab primitive；helper 不扩成 runner / queue / gate | `npm test`、`npm run typecheck` |
| `contracts/app_workbench_projection.json` | n/a | `already_thin_adapter` | contracts tests, OPL App consumption refs | refs/status/receipt/blocker projection contract | 已声明 App/workbench owner is OPL; OMA only supplies refs；不是 repo-owned workbench 或 compatibility facade | OMA work-order/candidate refs | App/workbench display shell | keep refs-only; live App screenshot/evidence from OPL/App side | `tests/contracts.test.ts` |
| `contracts/opl_domain_manifest_registration.json` | n/a | `already_thin_adapter` | contracts tests, OPL registry/discovery refs | registry discovery metadata | registry owner is OPL; OMA supplies registration refs only | domain descriptor/handoff refs | registry/discovery owner | OPL registry receipt required before live claim | `tests/contracts.test.ts` |
| `contracts/default_caller_deletion_evidence.json` | n/a | `already_thin_adapter` | contracts tests, OPL generated/default caller read model | default caller typed blocker、no-forbidden-write、tombstone/provenance 和 retained domain authority refs | 只为 OPL generated/default caller consumption 提供 domain-owned boundary evidence；不授权 repo-owned default wrapper、sidecar、workbench、generated shell、compatibility facade 或 physical delete | agent-building semantics、typed blocker refs、no-forbidden-write refs、target domain truth boundary refs | generated/default caller read model、hosted workbench/status projection、sidecar dispatch、physical cutover policy | OPL side consumption receipt 可继续关闭 evidence tail；本仓不得恢复 local wrapper 或把 default caller evidence 写成 active surface owner | `tests/contracts.test.ts` |
| `runtime/authority_functions/meta-agent-authority-functions.json` | n/a | `domain_authority_retained` | contracts tests, authority function refs | explicit authority refs for package builder and proposal authorizer | minimal refs-only authority functions belong OMA, but cannot own runtime | candidate package builder, proposal authorizer refs | none except OPL invocation shell | keep no generic owner flags; scripts must match implementation refs | `tests/contracts.test.ts` |

## Retired Legacy Surfaces

| retired surface | 退役证据 | 替代 owner / surface | 验证入口 |
| --- | --- | --- | --- |
| `plugins/opl-meta-agent/**` | 只由 repo-local installer 和 dedicated plugin test 保活；不在 action/stage contracts、package scripts 或 generated interface test 中作为 active caller | OPL Framework 从 `contracts/action_catalog.json` 与 `contracts/stage_control_plane.json` 生成 Skill/product-entry/tool descriptors | `tests/contracts.test.ts` 的 generated interface 与 no local wrapper 断言 |
| `scripts/install-codex-plugin.ts` | 仅写本仓 `.agents/plugins/marketplace.json`，属于 repo-owned local plugin wrapper materializer；已无 allowed active caller | OPL registry / generated surface handoff | `runtime/authority_functions` script morphology 断言 |
| `tests/codex-plugin.test.ts` | 只测试上述 repo-local wrapper 安装路径，属于兼容测试 | `tests/contracts.test.ts` 验证 OPL generated interfaces 和 manifest registration refs | `npm test` |
| `runtime/sidecar/`, `runtime/projection_builders/`, `runtime/lifecycle_adapters/` | 仅包含占位 README，无 active caller 或 authority function refs | `contracts/generated_surface_handoff.json`、`contracts/app_workbench_projection.json` 与 OPL-hosted runtime surfaces | runtime source shape 断言 |

## Bad-smell flags

- Large script-level policy: `agent-evidence-takeover.ts` 已把 proposal/blocker/work-order 输出组装收薄到 `scripts/lib/agent-evidence-materializer.ts`，入口仍承载 production evidence intake、suite generation、OPL Agent Lab invocation 和 artifact 落盘；`improve-from-agent-lab-suite.ts` 已把 target improvement mapping / patch traceability / editable surface inference 收薄到 `target-improvement-policy.ts`，并把 developer patch work-order / efficiency typed blocker 输出组装收薄到 `external-suite-materializer.ts`；`bootstrap-sample-agent.ts` 已把 sample / real-target domain-pack writer 收薄到 `scripts/lib/bootstrap-domain-packs.ts`；`scripts/lib/meta-agent-loop.ts` 已拆为 thin re-export、IO/OPL client、AI reviewer gate 和 suite/receipt/proposal builders。后续仍需防止 materializer helper 回长成私有 promotion/workbench/scaffold generator。旧 `work-order-policy.ts` 已拆为小型语义 helper，避免 work-order closeout / runtime consumption / workspace proof / readiness completeness / generic refs intake repetition 重新堆成单文件私有控制面。
- Agent Lab runner leakage: scripts may call OPL Agent Lab but cannot become Agent Lab implementation.
- Promotion shortcut risk: mechanism patch proposal and suite pass remain proposal-only / gated.
- Domain-specific command risk: OMA must not add MAS/MAG/RCA-specific command families; targets provide standard handoff.
- Same executor self-review risk: independent Codex reviewer requires separate invocation/context/task/receipt.
- Tests must not treat suite pass, generated surface proof or contract completeness as default promotion or target domain ready.

## Immediate thinning items

1. `scripts/lib/work-order-policy.ts` 已退役为自然拆分的 `work-order-policy-constants.ts`、`work-order-refs.ts`、`work-order-efficiency.ts`、`work-order-builders.ts`、`work-order-validation.ts`，分别承载 constants、refs intake、efficiency refs、readiness builders 与 fail-closed validator；不运行 Agent Lab、不签 promotion、不持有 target truth。
2. `scripts/lib/target-improvement-policy.ts` 已落地为 `improve:external-suite` 的 refs-only target improvement policy helper，入口脚本不再私有承载 change-ref mapping / patch traceability / editable surface inference。
3. `scripts/lib/agent-evidence-materializer.ts` 已落地为 `agent:evidence` 的 refs-only output materializer helper，入口脚本不再私有承载 owner receipt refs、candidate、developer work order、mechanism proposal 或 typed blocker assembly。
4. `scripts/lib/external-suite-materializer.ts` 已落地为 `improve:external-suite` 的 refs-only output materializer helper，入口脚本不再私有承载 developer patch work-order 或 efficiency typed blocker assembly。
5. `scripts/lib/bootstrap-domain-packs.ts` 已落地为 bootstrap smoke 的 refs-only pack writer helper，入口脚本不再私有承载 sample / real-target action/stage pack assembly。
6. `scripts/lib/meta-agent-loop.ts` 已从聚合 helper 降为薄 re-export；`meta-agent-loop-io.ts`、`meta-agent-loop-ai-reviewer.ts` 与 `meta-agent-loop-receipts.ts` 是 thinner migration input，不代表 OPL 接管完成。
7. Keep the scripts as materializers that consume helper policy and target refs.
8. Continue migrating stable AI reviewer gate / capability mapping / proposal policy into `agent/prompts/`, `agent/stages/`, `agent/quality_gates/`, `contracts/` or `runtime/authority_functions/`.
9. Do not add repo-local scheduler, queue, attempt ledger, status/workbench shell, registry owner, Agent Lab runner or promotion gate.

## OPL primitive dependencies

- Agent Lab suite runner and evolution/promotion gate;
- generated interface bundle and standard scaffold generator;
- pack compiler default domain-pack materializer for bootstrap / real-target smoke specs;
- registry/discovery and App/workbench drilldown live consumption;
- stage attempt ledger, queue, provider receipt and observability;
- target-agent generic handoff vocabulary across MAS/MAG/RCA.

## Forbidden claims

- `opl-meta-agent` work order does not sign target owner receipt.
- suite pass, schema completeness, generated surface readiness or mechanism proposal cannot promote default agent.
- OMA cannot write target domain truth, memory body, artifact body, quality/export verdict or owner receipt authority.
- registration/App projection readiness is not live App rendering evidence.
