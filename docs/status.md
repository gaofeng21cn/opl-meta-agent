# opl-meta-agent 状态

Owner: `opl-meta-agent`
Purpose: `current_status_and_evidence_boundary`
State: `active_truth`
Machine boundary: 本文是人读状态摘要。机器真相继续归 `contracts/`、`agent/`、`runtime/authority_functions/`、source、CLI/API 行为、OPL Agent Lab result refs、work-order receipts 和测试。

当前状态：已创建独立 OPL-compatible Foundry Agent repo，命名为 `opl-meta-agent`，并通过 OPL standard domain-agent scaffold validation、self-learning loop smoke、real-target delivery minimum evidence、registry/App refs-only readiness 与 mechanism patch proposal smoke。当前设计不再停留在 contract-ready：AHE-style developer work order 机器面已经进入 `improve:external-suite` 与 `agent:evidence`，可以把 blocked suite / production evidence handoff 转成带 failure evidence、root cause、targeted fix、predicted impact、allowed editable surfaces、verification refs、rollback/version refs、owner route refs 和 no-forbidden-write proof 的 patch-loop work order 或 typed blocker。2026-05-21 后续增强把 executable work order bundle 固定为 executor-first：work order 顶层和 completeness 都必须投影 `executor_lease_ref`、`reviewer_pool_refs`、`patch_execution_bundle_ref` 与 `target_closeout_refs`，独立 Codex reviewer pool 必须绑定 direct evidence、独立 invocation/context/trace/receipt、source refs、critique/suggestion/verdict 和 rollback/canary/version refs；缺关键 refs 时 fail closed typed blocker。2026-05-20 首个真实目标 smoke 已由 MAG 收口为 target owner patch-loop closeout projection，证明 OMA work order 的 11 个 closeout refs 可被真实 target agent 消费并返回 owner receipt / typed blocker shape；该证据仍不能替代目标 domain ready、quality verdict、artifact readiness 或 default promotion。

已落地：

- 标准目录：`agent/`、`contracts/`、`runtime/`、`docs/`。
- 可验证 domain pack：`agent/knowledge/`、`agent/prompts/`、`agent/quality_gates/`、`agent/skills/`、`agent/stages/` 下的分项语义文件均为当前合同列出的真实 pack 文件；README 只作为人读入口，不作为 `required_domain_pack_paths`。测试会检查这些 required files 存在、非空、无占位。
- 合同：descriptor、stage control plane、action catalog、memory descriptor、artifact locator、owner receipt、functional privatization audit。
- OPL generated interface 合同：pack compiler input、generated surface handoff、private functional surface policy。
- OPL domain manifest registration 合同：`contracts/opl_domain_manifest_registration.json` 把 domain descriptor、stage/action contracts、pack compiler input、generated surface handoff、authority function refs、App workbench projection 和 scaleout evidence contract 汇成 refs-only registration surface，并带 `opl_domain_manifest_discovery_receipt`；OPL domain registry / discovery / generated interface bundle 是消费方，本仓不持有 registry owner 或 generated surface owner。
- App/workbench projection 合同：`contracts/app_workbench_projection.json` 只投影 target brief、candidate package、Agent Lab result、developer work order、mechanism patch proposal、scaleout evidence 的 refs/status/receipt/blocker，并带 `opl_app_workbench_drilldown_readiness_receipt`；OPL App/workbench 持有展示 shell，本仓不能写 App runtime state、target truth、memory body、artifact body、quality/export verdict 或默认 promotion。该 readiness receipt 与 OPL App/operator drilldown 的结构消费均不宣称 App live rendering 或 production consumption 已完成。
- Real target agent scaleout evidence 合同：`contracts/real_target_agent_scaleout_evidence.json` 定义真实目标 agent delivery、blocked suite -> developer work order、多目标 scaleout 的必需 refs 和 no-forbidden-write proofs；当前已支持 `real_target_agent_delivery_count_min=1` 的 receipt/ledger 产出，并用 MAS + MAG 两个真实目标的 owner receipt / typed blocker / no-forbidden-write / cleanup closeout refs 关闭 `multi_target_scaleout` 证据门。该 closeout 仍不把 sample smoke、testing takeover smoke、suite pass、proposal 或 provider completion 写成 domain ready / quality verdict / default promotion。
- Production acceptance evidence：`contracts/production_acceptance/meta-agent-production-acceptance.json` 已把 `production_live_soak_not_claimed_by_conformance` / `domain_ready_not_claimed_by_conformance` 收口为 external-agent takeover/improve loop 的 domain-owned acceptance receipt，并记录 real-target delivery minimum receipt surface。它记录 structural/physical conformance 已通过、intake/test handoff/proposal materializer/review-audit receipt chain 已存在、promotion 仍受 gate 约束，并要求默认生成的新 agent 携带 morphology/conformance fixture。`contracts/production_acceptance/oma-production-consumption-long-soak-typed-blocker.json` 进一步把 OPL App/operator drilldown 的 `long_soak_refs` 缺口声明为 OMA-owned typed blocker；该 blocker 只证明 production-consumption long-soak 尚未落证，不能关闭 long-soak gate、不能声明 production ready，也不能写 OPL runtime state。
- `stage_control_plane` 路径解析：当前每个 stage 的 prompt、skill、knowledge、evaluation refs 都指向 repo-tracked `agent/prompts/*.md`、`agent/skills/*.md`、`agent/knowledge/*.md`、`agent/quality_gates/*.md` 文件；测试会确认每个 locator 指向真实文件。
- Stage executor policy candidate：`contracts/stage_control_plane.json` 已为每个 stage 增加 `executor_policy_capability` 与 `stage_executor_policy_candidates`，并继续保持 `selected_executor=codex_cli`。OMA 只声明 refs-only policy candidate；OPL / Agent Lab 持有 `agent_lab_stage_executor_policy_read_model`、试验执行、policy gate、stage attempt receipt 和 executor receipt。非默认 executor 必须显式 adapter / binding / receipt，缺 binding ref 时以 `blocker:missing_non_default_executor_binding_ref` fail closed；candidate 不能声明质量等价，OMA 不能直接切默认 executor。
- 已覆盖的非默认 executor candidate：`eval-suite-build` 有 `antigravity_cli` + `gemini-3.5-flash` + `high` + `google` + `executor-binding:antigravity/opl-meta-agent-eval-suite-build-html-authoring` 的 HTML / Agent Lab suite authoring candidate；`web-experience-research` 有 `claude_code` reviewer-diversity candidate，但 `executor_binding_ref=null`，因此只能作为缺 binding 的 fail-closed candidate，不能启动试验。
- 十阶段 meta-agent plan：intent intake、web experience research、stage decomposition、agent skeleton build、eval suite build、baseline run、external agent takeover、optimizer iteration、baseline delivery、online learning。
- 最小 repo-local test：`npm test`。
- 自举闭环脚本：`npm run bootstrap:sample -- --output-dir <dir> --opl-bin <opl> --ai-reviewer-evaluation <reviewer-eval.json>`；baseline delivery 缺少结构化 AI reviewer evaluation 时 fail closed，不签 baseline delivery receipt。该脚本还会生成 `real-target-delivery-receipt.json` 与 `real-target-scaleout-evidence-ledger.json`，用非 sample 的 `real-target-brief-agent` 证明 real-target delivery minimum evidence surface。
- 外部 agent 测试接管脚本：`npm run takeover:test -- --agent-dir <existing-agent-dir> --output-dir <dir> --opl-bin <opl>`。
- 外部 Agent Lab suite 自进化入口：`npm run improve:external-suite -- --suite <suite.json> --target-agent-dir <agent-dir> --output-dir <dir> --opl-bin <opl> --ai-reviewer-evaluation <reviewer-eval.json>`；用于把目标 domain 的 blocked suite 与同一结构化 AI reviewer evaluation 转为 developer patch work order、机制补丁、online-learning candidate 和目标 agent capability improvement candidate。`opl-meta-agent` 可以据此作为开发者修改目标 agent 的源码、测试和文档，但不能写目标 domain truth、quality verdict、memory body 或 artifact body。
- domain agent production evidence tail 接管入口：`npm run agent:evidence -- --agent-repo <agent-repo> --output-dir <dir> --opl-bin <opl> [--ai-reviewer-evaluation <reviewer-eval.json>]`；读取目标 agent 的 `production_acceptance`、`agent_lab_handoff`、generated-surface handoff 与 owner-receipt 合同，生成 `suite_kind=agent_production_evidence_suite` 的 `agent-lab-suite.json`，调用 OPL Agent Lab `run --suite`，再输出 `developer-patch-work-order.json`、`target-capability-improvement-candidate.json`、`mechanism-patch-proposal.json` 或缺 reviewer 时的 `typed-blocker.json`。该入口只写 refs/proposal/work-order，不写目标 domain truth、quality/export verdict、artifact body、memory body 或 owner receipt authority。
- AHE-style developer work order：`improve:external-suite` 与 `agent:evidence` 的 work order 已要求 failure evidence、root cause、targeted fix、predicted impact 四字段，并带 target repo file hints、allowed editable surfaces、required verification refs、rollback/version refs、owner route refs、no-forbidden-write proof 和 target runtime/read-model consumption verification。缺 structured AI reviewer evaluation、缺 source refs、缺 predicted impact、缺 required verification refs 或缺 no-forbidden-write proof 时 fail closed，输出 typed blocker；suite pass、schema completeness、generated surface proof 或 OMA work order 本身都不能授权目标 domain ready、quality verdict、artifact readiness 或 default promotion。
- Efficiency work order：`improve:external-suite` 与 `agent:evidence` 现在把 external suite 或 production evidence 中的效率非回归证据投影为 target-agent generic `efficiency_non_regression_refs`，覆盖 `quality_floor_refs`、`latency_baseline_refs`、`usage_cost_refs`、`cache_reuse_refs` 和 `target_verification_refs`，并同步进入 developer work order、work order completeness、machine closeout refs 和 required verification refs。输入可以是显式 `efficiency_non_regression_refs`，也可以是标准 `*_efficiency_handoff_projection` 的 refs-only projection；出现 latency / usage cost / cache reuse / target verification 证据但缺 `quality_floor_refs` 时 fail closed typed blocker，不输出 executable work order 或 mechanism patch proposal。该能力只消费标准 target handoff / evidence refs，不新增 RCA、MAS、MAG 或其他 domain 专用 command family。
- Patch-loop closeout machine refs：developer work order / typed blocker / App projection contract 现在要求 `machine_closeout_refs` 覆盖 `blocked_suite_result_ref`、`developer_patch_work_order_ref`、`patch_traceability_matrix_ref`、`target_repo_verification_refs`、`target_runtime_read_model_consumption_ref`、`workspace_environment_proof_ref`、`no_forbidden_write_proof_ref`、`target_owner_receipt_or_typed_blocker_ref`、`patch_absorption_ref`、`worktree_cleanup_ref` 和 `agent_lab_re_evaluation_ref`。缺任一类关键 refs 时应 fail closed 为 typed blocker，而不是输出可执行 patch order。
- Agent Lab / OMA 标准边界：Agent Lab 是 OPL Framework 的通用 refs-only suite runner / mechanism / evidence control plane；OMA 是通用 target-agent foundry / repair / takeover consumer。MAS、MAG、RCA 等目标 agent 应提供标准 handoff 供它们消费，不能把状态写成 Agent Lab 或 OMA 分别兼容某个 domain agent。
- 自进化职责分工：Agent Lab 持有 evidence / root cause / targeted fix / predicted impact / next-run falsification read model、best-of-N variant comparison 和 risk-tiered promotion gate；OMA 持有 developer work order、target capability candidate、mechanism patch proposal 和 typed blocker；目标 agent 持有 owner receipt、domain truth、quality verdict、artifact authority 和 memory body。可用闭环的验收路径是 `blocked handoff -> Agent Lab run/evolve -> OMA work order -> Codex allowed patch -> target verification -> owner receipt -> Agent Lab re-evaluation`。
- `skillnerds/xskill` clean-room 学习已作为文档级决策吸收：OMA 学习 trajectory atomization、candidate buffer、per-skill version / canary evidence、team redaction / sync refs，用于收紧 skill / prompt / policy candidate、developer work order、mechanism proposal 和 reviewer evidence 的表达。当前状态不包含 xskill daemon/runtime、generic scheduler、skill installer、team server、contracts/tests/package/scripts 变更，也不改变 OPL Framework 对 Agent Lab、promotion gate、queue、worktree lifecycle、absorb / cleanup 和 generated interfaces 的 owner 权限。
- OPL 统一接口投影：`opl agents interfaces --repo-dir <this-repo> --json` 可从本仓 contracts 生成 CLI、MCP、Skill、product-entry、OpenAI tool 和 AI SDK 描述；本仓不持有私有通用入口包装层。
- 生成接口权限边界：`pack_compiler_input` 与 `generated_surface_handoff` 明确 domain pack 只供应领域 refs，OPL Framework 生成接口只负责 invoke/project，不写 domain truth、memory body、artifact body、quality/export verdict，也不能由本仓声明 generated surface ownership。
- Registration / App / evidence handoff：`generated_surface_handoff`、`action_catalog` 和 `functional_privatization_audit` 已引用 domain manifest registration、App workbench projection 与 real target agent scaleout evidence contract，作为后续 OPL registration、product projection 和 evidence review 的稳定消费面。
- Self-learning loop smoke：生成 `sample-brief-agent`，补齐最小 action/stage domain pack，调用 OPL scaffold validate，调用 `opl agents interfaces --repo-dir <sample-agent>` 生成统一接口包，写入 Agent Lab external suite，通过 `opl agent-lab run --suite` 得到 `passed`，再产出 baseline delivery receipt、gated online-learning candidate 与 `mechanism-patch-proposal.json`。
- Testing takeover smoke：读取既有 OPL-compatible agent descriptor/contracts，生成 `agent_lab_external_suite`，调用 OPL Agent Lab，通过后产出 `testing_takeover_self_evolution_receipt`、gated self-evolution online-learning candidate 与 `takeover-mechanism-patch-proposal.json`；不写 target memory body，不接管 target domain truth / quality / artifact authority，不无 gate promote default agent。
- External-suite self-evolution smoke：读取目标 domain 按标准 Agent Lab contract 生成的 suite，调用 OPL Agent Lab，若 suite blocked 则产出 `meta-agent-improvement-receipt.json`、`online-learning-candidate.json`、`mechanism-patch-proposal.json`、`target-capability-improvement-candidate.json` 与 `developer-patch-work-order.json`；该 work order 管理目标 agent 源码补丁、测试、文档、吸收回主线和临时 worktree 清理要求，同时保持 domain truth / quality / artifact authority 不越权。live / production acceptance 只是该标准机制的一类目标 agent handoff：目标 agent 提供 owner refs / acceptance refs，OMA 生成 no-patch / patch work order 与 owner-gated proposal，最终 owner receipt 仍由目标 domain agent 签发。
- Developer work-order traceability：blocked suite 的 rubric gap 会被映射为 `patch_traceability_matrix`，包含 source failure refs、required patch refs、editable surfaces、目标仓文件提示、测试/receipt/禁止写入证明、runtime/read-model consumption verification 和版本 closeout 要求，避免 self-evolution 退化成泛泛 proposal 或前台开发者自由发挥。
- AI reviewer evaluation consumption：baseline delivery 和 external-suite improvement 共用结构化 reviewer evaluation schema，要求 reviewer_kind、model_or_provider、run_ref、critique、suggestions、source_refs、verdict、provenance；空 critique/suggestions 或 source_refs 只有 suite/scaffold refs 时 fail closed。
- Codex-first semantic pack：`agent/knowledge/opl-boundary-policy.md`、核心 prompts 和 mechanism adoption gate 已明确 executor-first / Codex-first 语义：最强 AI executor 负责专家判断、问题重构、反例搜索、工具/knowledge 缺口发现和路线取舍；合同只保边界、安全、receipt、projection 和 fail-closed 条件，scorecard / suite pass / contract completeness 不能替代独立 AI reviewer 或 owner verdict。
- Reviewer suggestion traceability：external-suite improvement 会把 AI reviewer critique/suggestions/source refs 投影到 target capability improvement candidate 与 developer patch work order，`patch_traceability_matrix` 为可识别 reviewer suggestion 标注对应 required patch refs、editable surfaces 和 target repo file hints。
- Workspace environment consumption proof：当目标 agent 修复依赖 runtime extras、workspace lock/profile 或 owner-entry 环境迁移时，developer work order 会要求 `target_workspace_environment_verification`、dependency lock/profile migration proof、owner-entry redrive proof 和 repo hygiene proof，避免只在源码测试通过而真实 study runtime 没有消费到新能力。
- Mechanism patch proposal：记录 `mechanism_ref/version`、`editable_surfaces`、`observe/diagnose/edit`、`segment_run_ref`、`evidence_delta_ref`、`next_mechanism_candidate_ref` 和 proposal-only authority boundary。
- Physical source morphology：`agent/` 是 agent-building semantic pack，`contracts/` 是 registration / generated handoff / App projection / scaleout evidence 机器面，`runtime/authority_functions/` 是唯一保留的 runtime source 子面，`scripts/` 只是 authority implementation refs、smoke helpers、fixture/proof helper 或 developer work-order materializer。本仓已移除 repo-local Codex plugin/Skill wrapper 与 `runtime/sidecar`、`runtime/projection_builders`、`runtime/lifecycle_adapters` 占位入口；CLI/MCP/Skill/product-entry、sidecar/projection、status read model 和 workbench 均由 OPL Framework 从 contracts 生成或托管。
- Fresh private-control-plane scan：2026-05-23 当前 repo-tracked 实现文件没有超过 800 行；超过 1000 行的是测试聚合面，不是 OMA 私有 runtime/status/workbench/product-entry/projection/attempt-ledger/state-machine 实现。实现侧最大文件是 `scripts/lib/agent-evidence-materializer.ts`，当前作为 refs-only developer work-order / proposal / typed-blocker materializer 保留；没有发现可低风险迁移或自然拆分的 generic implementation。后续若该 materializer 接近 800 行，优先按 owner receipt refs、capability candidate、developer work order、mechanism proposal 与 typed blocker 分段，不新增兼容层。
- Standard Agent physical conformance scan：2026-05-24 当前 tracked tree 只保留 `agent/`、`contracts/`、`runtime/authority_functions/`、`scripts/`、`tests/`、`docs/` 和 branding assets；没有 tracked repo-local `plugins/`、`.agents/`、`.codex/`、runtime sidecar/projection/lifecycle adapter、CLI/MCP/product-entry/workbench wrapper，`package.json` 也不暴露 `bin`、`main` 或 `exports`。`contracts/pack_compiler_input.json` 的 `minimal_authority_functions` 已与 `runtime/authority_functions/meta-agent-authority-functions.json` 和 functional audit 对齐，只声明 `candidate_agent_package_builder` 与 `mechanism_patch_proposal_authorizer`；历史 scaffold 名称不再作为机器 authority surface。
- 私有实现迁移治理入口：[opl-meta-agent 私有实现与 OPL 迁移台账](./active/opl-private-implementation-migration-inventory.md) 记录大型 script/materializer 面的 active caller、分类、保留 authority、可上收 OPL 子域、拆分门槛与验证入口。2026-05-21 已把 `agent:evidence` 与 `improve:external-suite` 共享的 target patch-loop machine refs、runtime/read-model consumption proof、workspace environment proof、no-patch/source-patch closeout evidence、默认 forbidden target surfaces、work-order readiness completeness、developer patch work-order validator、generic refs intake / owner-route normalization / evidence refs extraction 先收薄到 work-order policy helper；2026-05-22 该 717 行 helper 已退役为 `scripts/lib/work-order-policy-constants.ts`、`work-order-refs.ts`、`work-order-efficiency.ts`、`work-order-builders.ts` 与 `work-order-validation.ts`，调用方直接消费小型语义模块。后续收薄又把 `improve:external-suite` 的 target improvement policy、change-ref mapping、patch traceability matrix、editable surface inference 和 target file-hint lookup 抽到 `scripts/lib/target-improvement-policy.ts`，入口脚本只保留 external suite consumption 与 candidate/work-order/proposal materialization。本轮又把 `scripts/lib/meta-agent-loop.ts` 从 754 行聚合 helper 降为薄 re-export，并拆出 IO/OPL client、AI reviewer gate、suite/receipt/proposal builder 三个相邻模块；这是 `needs_split_before_migration -> thinner migration input`，不声明 OPL 已完全接管，不新增兼容层。上述 helper 只作为 developer work-order / target improvement refs materializer，不持有 Agent Lab runner、promotion gate、queue、attempt ledger、registry、App shell 或 target truth authority。
- Usable landing patch-loop 口径：当前 OMA 已能从真实 target agent handoff 生成带 AHE 四字段和 patch controls 的 developer work order。下一类证据不再是继续补 contract，而是在真实目标仓执行该 work order：Codex 只改 allowed editable surfaces，重跑目标 verification，记录 target runtime/read-model consumption、workspace environment proof、no-forbidden-write proof、owner receipt、patch absorption 和 cleanup closeout。独立 Codex reviewer attempt 必须以 direct evidence、no-shared-context、rollback/canary/version refs 和 reviewer provenance 为输入，不能用 schema completeness、suite pass、generated surface proof 或 work order 结构完整性替代专家 verdict。work order 现在显式消费 OPL work-order execute primitive 的 executor lease / reviewer pool / patch bundle / target closeout refs；OMA 只做薄 materializer 和 typed blocker，不持有 Agent Lab runner、promotion gate、queue、attempt ledger、registry、App shell 或 target truth authority。
- External work order execution delegation：`npm run execute:external-work-order -- --work-order <work_order_path> --opl-bin <opl_bin> [--output <output_path>]` 已作为 OMA 薄入口落地。该入口只校验 `opl_meta_agent_developer_patch_work_order` 的 executor-first refs 与 forbidden authority boundary，然后委托 `opl work-order execute --work-order <path> --json`；target worktree lifecycle、generic runner、queue、attempt ledger、absorb、cleanup、post-absorb target-domain owner closeout hook 调用和 execution receipt 继续由 OPL Framework / target owner 控制面持有。delegation receipt 明确写入 `owner_closeout_hook_delegated=true`、`target_owner_closeout_owner=target-domain via OPL`、`oma_can_write_owner_receipt=false`，证明 OMA 只委托 hook，不写 owner receipt body。

未完成：

- OPL registry 侧真实 discovery 消费仍需由 OPL 主仓或 App 侧记录外部 receipt；本仓已提供 refs-only discovery receipt surface，OPL generated interface read-model 已能消费本仓 contracts 并返回 `status=ready`。
- App/workbench 侧 OPL drilldown 已消费 OMA patch-loop refs，当前读模型显示 `opl_meta_agent_patch_loop_closed_count=2`、`opl_meta_agent_scaleout_target_count=2`，且 OPL 侧已可把 OMA production-consumption refs-only gate 读成 ready。这个 ready 只说明 OPL consumption gate 可关闭，不授权 target domain ready、family production ready、default promotion 或未来 target cohort 自动通过。后续仍需要真实 registry discovery receipt、App render/screenshot、live runtime drilldown closeout、repeat long-soak 和更多 target patch-loop owner receipt / typed blocker 样本。
- Stage executor policy candidate 仍只是 OMA 侧 candidate contract；真正的跨 executor 试验、gate 结果、receipt 和默认 executor promotion 需要 OPL / Agent Lab 侧消费后落证。`web-experience-research` 的 `claude_code` candidate 当前明确缺 binding ref，状态应保持 fail closed。
- 多目标真实 agent scaleout 的 OMA refs-only 证据门已由 MAS + MAG 两个真实目标 closeout refs 关闭；MAG 已提供首个真实 target patch-loop closeout projection。后续剩余是继续扩展到 RCA / 新 Foundry Agent 的更多 target-agent handoff 样本，并积累更多真实 patch / rerun / owner receipt 样本。该 evidence 不授权默认 promotion。
- MAS/MAG/RCA 的标准 handoff vocabulary 仍需继续收敛；目标是每个标准 OPL Agent 自己提供 Agent Lab / OMA 消费接口，而不是在 Agent Lab 或 OMA 内增加 domain-specific suite kind / command。
- `scripts/` 中仍在增长的 agent-building policy 必须持续迁回 `agent/`、`contracts/` 或 explicit authority refs；允许保留的脚本只能是 authority implementation、smoke helper、fixture/proof helper 或 developer work-order materializer。
- 仍保留的 `bootstrap:sample`、`takeover:test`、`improve:external-suite`、`agent:evidence` 与 `scripts/lib/*` 有 active npm/test caller，当前只能作为 smoke/proof/materializer 或 minimal authority implementation 保留；证据不足以删除时继续由私有实现迁移台账跟踪，不能伪装成 generic runtime 或 promotion engine 已完全迁出。
